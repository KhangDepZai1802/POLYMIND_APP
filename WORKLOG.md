# 📓 WORKLOG — POLYMIND OLMS (file phối hợp giữa các session AI)

> **File này là nguồn sự thật chung cho Claude và Codex.** Đọc TRƯỚC khi làm, cập nhật SAU khi làm.

---

## ⚠️ QUY TẮC BẮT BUỘC (đọc mỗi lần)

1. **TRƯỚC khi bắt đầu session:** đọc hết file này — phần `TRẠNG THÁI HIỆN TẠI` + `VIỆC TIẾP THEO` + entry mới nhất trong `NHẬT KÝ`.
2. **Chọn việc:** làm theo `VIỆC TIẾP THEO`. Nếu muốn làm khác, ghi rõ lý do vào nhật ký.
3. **SAU khi làm xong (hoặc trước khi hết session):**
   - Cập nhật `TRẠNG THÁI HIỆN TẠI` (1-2 dòng).
   - Cập nhật `VIỆC TIẾP THEO` (việc kế cho người sau — càng cụ thể càng tốt).
   - **Thêm 1 entry mới** vào đầu `NHẬT KÝ SESSION` (mới nhất ở trên).
   - **GIỮ GỌN (bắt buộc):** chỉ giữ **6 mục gần nhất** ở `TRẠNG THÁI HIỆN TẠI` và **6 entry gần nhất** ở `NHẬT KÝ SESSION`. Khi thêm mục/entry mới → **xóa mục/entry cũ nhất** để file không phình to (đỡ tốn token đọc lại mỗi phiên).
   - Nếu có blocker/lỗi chưa fix → ghi vào `BLOCKERS`.
4. **Luôn để app build được** (`dotnet build Polymind.slnx`) trước khi kết thúc. Nếu để dở, ghi rõ "đang dở, chưa build".
5. Chi tiết backlog đầy đủ + bẫy kỹ thuật + quy ước code: xem **docs/05-handoff-codex.md** (đọc 1 lần để nắm nền).

**Format 1 entry nhật ký:**
```
### [YYYY-MM-DD] Session N — <Claude|Codex>
- **Làm được:** ...
- **File thay đổi chính:** ...
- **Đã test:** ... (build? chạy? smoke test gì?)
- **Lưu ý/cảnh báo cho người sau:** ...
```

---

## 🎯 TRẠNG THÁI HIỆN TẠI

- **CỔNG CÁ NHÂN HÓA PHỤ HUYNH & HỌC SINH (mobile-first) + 4 việc phát sinh (Session 60, Claude) — CODE XONG, BUILD 0/0, MIGRATION ĐÃ ÁP, WEB ĐANG CHẠY :5177, CHƯA duyệt mắt/commit.** (1) **DB:** thêm `Candidate.ParentUserId` + migration `AddCandidateParentUser` (đã áp — cột `parent_user_id` tồn tại) → 1 ứng viên gắn tối đa **3 tài khoản** (CTV/TVV, Học sinh `OwnerUserId`, Phụ huynh `ParentUserId`). (2) **Scope/quyền:** `AgentScope` resolve owned candidate qua `OwnerUserId||ParentUserId` + cờ `IsParent/IsStudent/OwnedCandidateHasLoan`; **DbSeeder** đổi quyền parent+student = `candidates/training/payments/loans/notifications:read + messaging` (parent **bỏ** agents/commissions — verify DB OK). (3) **Điều hướng mobile:** `NavMenu` nhánh self-scoped (Tổng quan/Hồ sơ/Đào tạo/Tài chính/Hỗ trợ vay[nếu vay]/Tin nhắn/AI, bỏ Hướng dẫn+admin), **bottom-nav** `PortalBottomNav.razor` (CSS ≤959px) trong MainLayout, `RedirectToLogin` đưa self-scoped về `/me`. (4) **Trang `/me` Tổng quan MỚI** (`Pages/Portal/Overview.razor`): thẻ XKLĐ/đóng tiền/đào tạo/job/vay. (5) **Cá nhân hóa:** `Candidates`+`Training` self-scoped nhảy thẳng chi tiết (bỏ bảng); `Finance`+`Loans` chỉ hồ sơ của mình; guard lọc cứng `OwnedCandidateId`. (6) **Tin nhắn theo quan hệ:** parent↔CTV/TVV+con; student↔CTV/TVV+phụ huynh (`BuildAllowedRecipientsAsync` + chặn server ở Send). (7) **AI giới hạn:** bỏ tab CV, chỉ nạp hồ sơ CHÍNH ứng viên + prompt cấm lộ dữ liệu người khác/ngoài phạm vi. (8) **CandidateDetail:** khu "Tài khoản đăng nhập" 3 dòng + `ParentAccountDialog` (audit link_parent/unlink_parent). **PHÁT SINH:** (a) **Sửa ghi âm** (`audio-recorder.js` trả **Blob** → hết lỗi "not a typed array or blob"; thêm khung **nghe lại** trước Gửi/Bỏ, KHÔNG tự gửi). (b) Trang Ứng viên **cột "Trạng thái"** (Ứng viên/Học viên/Lao động theo `PersonTitle`) + Giới tính **ẩn chữ giữ icon ở laptop** (`laptop-hide-inline` ≤1600px). (c) **Đào tạo mở rộng:** Training list + card ở CandidateDetail hiện cho **mọi ứng viên đã đặt cọc** (`PersonTitle.IsTitled`) → 14 người thay vì 3. (d) **Gemini/Render:** thông báo lỗi key nêu đặt env `Ai__Gemini__ApiKey` trên Render (key production PHẢI set ở dashboard Render, KHÔNG commit). (e) **Tạo tài khoản Đại lý/CTV** ở `/agents/{id}`: nút "Tạo tài khoản đại lý" ở đầu trang + nút "Tạo tài khoản" từng hàng CTV (`PartnerAccountDialog.razor` — role agent/collaborator, set `Agent/Collaborator.UserId`, audit link_account); đã có tài khoản → nút **disable "Đã có tài khoản"** (verify DB: 5/5 đại lý + 13/13 CTV đã có tài khoản → tất cả disable). **TINH CHỈNH cuối phiên:** (f) `RedirectToLogin` đưa Đại lý/CTV về `/agents`, Phụ huynh/Học sinh về `/me` → **hết cảnh non-super-admin thấy "Không có quyền truy cập" sau đăng nhập** (partner/self-scoped landing "/" trước đây bị access-denied). (g) CSS bảng ứng viên: thu gọn padding cột (chữ đỡ xuống dòng) + laptop ≤1600px **chip giới tính ép về hình tròn** (hết méo icon). (h) Trang `/admin/parents-students` **GIỮ nút vào + bảng quản lý**, chỉ **ẩn card "Tạo tài khoản"** (thêm param `AccountManagerPanel.ShowCreateForm=false`) vì tạo trực tiếp ở chi tiết ứng viên. (i) Overview: tài khoản Phụ huynh chào **"Phụ huynh em <tên>"**. Tin nhắn parent/student **không** thấy super admin (đã đúng từ đầu). **ĐÃ ĐỒNG BỘ DB LÊN RENDER** (`sync-db-to-render.ps1` — Render 41 users, có cột `parent_user_id` + quyền mới). ⚠️ **CODE mới CHƯA lên Render** (chỉ DB) → phải redeploy app từ git để thấy cổng cá nhân hóa + fix landing; và đặt env `Ai__Gemini__ApiKey` trên Render.
- **TRANG RIÊNG "PHỤ HUYNH & HỌC SINH" (Session 59, Claude) — CODE XONG, BUILD 0/0, WEB ĐANG CHẠY :5177, CHƯA duyệt mắt/commit.** Tách phần quản lý tài khoản thành component dùng chung `AccountManagerPanel.razor` (tham số `ManagedRoles` + `RolesFixed` + `AllowDelete`). Tab **Tài khoản** ở `/admin` giờ quản lý mọi role TRỪ parent/student (`StaffRoles`) + có nút **"Phụ huynh & Học sinh"** → trang mới **`/admin/parents-students`** (`ParentStudentAccounts.razor`) chỉ tạo/quản lý parent+student. **Cập nhật theo yêu cầu user:** trang P&S **KHÔNG khóa cố định** nữa — cho **đổi qua lại parent↔student** (đổi role có hộp `ConfirmPasswordDialog` xác nhận); super admin có nút **Xóa** tài khoản (`AllowDelete=true`, luôn hỏi xác nhận `ShowMessageBoxAsync`, dọn `Candidate.OwnerUserId` trước khi `UserManager.DeleteAsync`, audit `delete`). Chốt chặn 2 chiều vẫn còn ở `CreateUserAsync`/`SaveUserRoleAsync` (không tạo/đổi ngoài `ManagedRoles`). **Không migration.**
- **ĐỒNG BỘ LOGIC TIỀN BẠC 3 TRANG: THU NỢ = CHỈ NỢ CÔNG TY (Session 58, Claude) — CODE XONG, BUILD 0/0, CHƯA restart/commit.** User làm rõ luồng tiền: (1) **Tài chính** giữ nguyên = nghĩa vụ đóng theo chi phí đơn (4 bước 20/30/30/20), **thủ công**, không tự động đánh dấu dù công ty ứng tiền. (2) **Vay ngân hàng** = người vay tự trả cho NH → **KHÔNG** lên trang Thu nợ; `LoanDialog` khi chọn *Ngân hàng* nay **ẩn** trừ lương/lịch trả góp, chỉ lưu thông tin (số tiền, NH, lãi, ngày giải ngân); lưu bank sẽ dọn `MonthlyDeduction/DeductionStart` + xóa `LoanRepayment` cũ. (3) **Nợ công ty** = tỉnh không cho NH vay, công ty cho vay để đóng tài chính, sau XKLĐ trả lại **gốc + lãi** → chỉ loại này hiện ở `/debt-collection` (lọc `Kind==Company`); số tiền nợ **prefill = chi phí đơn hàng** (cho sửa); lịch trả góp nay chia **gốc + lãi đơn** (lãi = gốc × %/năm × số tháng/12). **Không migration** (chỉ đổi logic UI + query). PHẢI restart web để nạp code.
- **8 GÓP Ý DUYỆT MẮT (Session 57, Claude) — CODE XONG, COMPILE 0/0, CHƯA restart/commit.** (1) Lỗi micro: `audio-recorder.js` bắt lỗi getUserMedia → message tiếng Việt rõ. (2) Chip **Học viên** (đơn du học) / **Lao động** (nhóm khác) theo `JobOrder.Category`, giữ ngưỡng B5 (`PersonTitle.cs`). (3) **MODULE ĐÀO TẠO** riêng: `/training` (danh sách + tiến độ) + `/training/{id}` (**timeline theo tuần** gom `TrainingEvaluation`), NavMenu "Đào tạo"; thẻ Đào tạo ở CandidateDetail rút gọn còn tóm tắt + link module. (4) **Nợ VG**: seeder tạo Loan `Kind=Company` (tỉnh không được NH vay, VG cho đi) + lịch trả góp → `/loans`+`/debt-collection`. (5) Lỗi thông báo trùng khóa 23505: lead-care nay **UPDATE hàng cũ** thay vì chèn (`NotificationService`). (6) Hoa hồng: đại lý **5%** (1/1.5/2.5% theo mốc), CTV **35%** (30-40%) — `AgentCommissionRates.cs` + seeder chuẩn hóa dữ liệu cũ + tính lại. (7) Bỏ menu "Thông báo" (giữ chuông góc phải). (8) Tràn bảng laptop: `WorkflowSteps.ShortLabel`+`StepChip` 2 span + CSS ≤1600px hiện nhãn rút gọn. **Không migration** (code+seeder). **PHẢI restart web** để nạp code + seeder chuẩn hóa hoa hồng/nợ VG.
- **BIÊN BẢN HỌP VIETGROUP 3/7 — ĐỢT 1 (3 module lớn) + ĐỢT 2 (quick-win) + RESET ACC CTV (Session 56, Claude).** Theo `POLYMIND HỌP 3_7_2026.pdf` + plan đã duyệt. **ĐỢT 1:** (1A) **Module Đào tạo** — enum `TrainingTrack`/`EvaluationRating`, entity `TrainingRecord`+`TrainingEvaluation`, thẻ "Đào tạo" ở `CandidateDetail` (2 ô Học tiếng/Chuyên môn + tiến trình %), dialog `TrainingEvaluationDialog` (chấm 4 tiêu chí Chuyên cần/Chuyên môn/Kỷ luật/Tài chính + đính kèm minh chứng MinIO) + `TrainingTrackDialog`; resource quyền `training`. (1B) **Công nợ & Thu nợ** — `LoanKind{Bank,Company}` + trường trừ lương + entity `LoanRepayment` (lịch trả góp); `LoanDialog` chọn loại nợ + sinh lịch; `Loans` thẻ "Nợ công ty" + lọc loại nợ; **trang `/debt-collection`** (Kế toán đánh dấu thu từng kỳ → trả đủ auto `Settled`). (1C) **Hoa hồng theo giai đoạn đóng tiền** — `CommissionEngine` sinh hoa hồng khi `PaymentStage` được đóng (giữ nguyên số tiền, đổi THỜI ĐIỂM), thêm `AgentCommission.Stage`; gọi ở `Finance.MarkStagePaid` + workflow advance; **bỏ** generator theo mốc workflow cũ. **3 migration:** `AddTrainingAndLoanRepayment`, `AddCommissionStage`, `AddCandidateOwnerUser` (đã sửa default `kind='Bank'`). **ĐỢT 2:** 2D danh xưng **"Lao động"** từ B5 (`PersonTitle`); 2F **ẩn SĐT với CTV** (mask `CandidateDetail`+`MyCommissions`) + **bỏ bảng thi đua CTV** khi tài khoản là CTV; 2G role **Phụ huynh** (=quyền CTV) + **Học viên** (self-only qua `Candidate.OwnerUserId`+`AgentScope.IsSelfScoped`); 2H **lọc quốc gia toàn danh sách** (thêm ô tìm ở `/jobs`, thêm Province/TargetCountry vào tìm `/leads`); 2E **sơ đồ cây** `/agents/tree` (Công ty→Đại lý→CTV). **RESET ACC CTV:** đã reset 13 tài khoản `ctv-*` về mật khẩu `Admin@123` (SQL update password_hash Identity V3 + verify Success), active + hết khóa, giữ 2FA. **2I Ghi âm tin nhắn (XONG):** module JS `wwwroot/js/audio-recorder.js` (MediaRecorder, trả bytes qua `IJSStreamReference` để né giới hạn 32KB SignalR); `IDocumentStorage.UploadMessageAudioAsync(bytes)` + mở whitelist audio (`.webm/.ogg/.mp3/.m4a/.wav`) ở `MinioDocumentStorage`; nút mic ở `Messages.razor` (ghi/dừng → chip "Ghi âm N giây" → Gửi) + nhánh `<audio>` player khi attachment `audio/*` + preview "🎤 Tin nhắn thoại". **UI gắn tài khoản Học viên (XONG):** `StudentAccountDialog` (tạo user `hocvien-<mã>@polymind.local` mật khẩu `Admin@123` role `student` + set `Candidate.OwnerUserId`; đặt lại mật khẩu / gỡ liên kết & khóa) mở từ nút **"Tài khoản Học viên"** ở `CandidateDetail` (gate `users:create`). **ĐÃ BUILD IN-PLACE `Polymind.slnx` 0 warning/0 error, 3 migration đã áp vào DB (verify bảng/cột/role/permission), web CHẠY `http://localhost:5177` `/login` 200 + `/js/audio-recorder.js` 200; reset 13 acc CTV verify Success.** **CHƯA commit.** Còn lại chỉ là DUYỆT MẮT bằng trình duyệt thật (đặc biệt ghi âm cần cấp quyền micro).
- **MỤC 5 QUY TRÌNH 20 BƯỚC + BẢNG THI ĐUA THEO THÁNG ĐẶT CỌC + SỬA 2FA + DOCX PHẢN HỒI (Session 55, Claude).** (1) Đổi workflow ứng viên **17→20 bước** đúng góp ý Vietgroup + bước phụ **7.5 "Chọn lại đơn hàng khác"** (rớt B8→lùi 7.5→bắt buộc gắn đơn MỚI mới sang B8, giữ hồ sơ). GIỮ tên member enum cũ nên **dữ liệu cũ tự map, không migration**. Tình huống theo bước: B8 chọn 1/3 (Thi tuyển|Phỏng vấn|Xét duyệt) có nút Rớt; B10 chọn nhiều nội dung học/Bỏ qua; B15 Thanh toán hoàn tất|Cam kết trả nợ; **B19 nhật ký tương tác xứ người** (ghi nhiều lần, dài hạn); **B20 chỉ chuyển khi vay đã tất toán** (thêm `LoanStatus.Settled`) + xác nhận thủ công. Helper mới `Display/WorkflowSteps.cs` (No/Next/Progress). (2) **Bảng thi đua `/agents`**: cột "Ứng viên" đếm **ứng viên đạt bước Đặt cọc TRONG THÁNG đang lọc** (sang tháng reset 0, cùng thời điểm hoa hồng). (3) **Sửa giao diện 2FA lệch trái** (`login-2fa`+`2fa-setup`) bằng wrapper `.login-solo` canh giữa mọi breakpoint + cache-bust `login.css`. (4) File **`POLYMIND - Phan hoi gop y Vietgroup Edu.docx`** (root) trả lời từng mục góp ý. Build **0 warning/0 error**, web `/login` 200, DB map đúng. **Chưa commit.** ⚠️ Chưa duyệt mắt trình duyệt thật luồng 20 bước + 2FA canh giữa mobile.
- **MỤC 1 BẢO MẬT VIETGROUP: 2FA TOTP BẮT BUỘC + SIẾT PHÂN QUYỀN (Session 54b, Claude).** User chốt: 2FA bằng **app Authenticator (TOTP)**, **bắt buộc TẤT CẢ tài khoản**. Đã làm: trang `/account/2fa-setup` (SSR, quét QR bằng QRCoder + nhập key tay + verify mã 6 số + hiện **8 mã khôi phục** 1 lần duy nhất); trang `/login-2fa` (mã 6 số hoặc mã khôi phục); `Login.razor` rẽ nhánh `RequiresTwoFactor`; **middleware bắt buộc**: user cookie chưa bật 2FA bị chuyển về trang cài đặt ở mọi request (cache trạng thái 60s/user — `TwoFactorStatusCache`, allowlist hạ tầng trong `TwoFactorEnforcement`); lockout sẵn có (sai 5 lần khóa 15 phút). Trang `/admin`: tài khoản role super admin hiện chip **"Cố định"** thay dropdown role + ẩn nút **Lưu role** (không ai đổi được role của super admin, chỉ dùng nút Sửa) + guard server-side; đổi role tài khoản khác phải qua **dialog nhập lại mật khẩu người thao tác** (`ConfirmPasswordDialog`); **xóa toàn bộ nút "Reset mật khẩu"**; dialog Sửa thêm nút **Đặt lại 2FA** (nhân viên mất điện thoại → cài lại ở lần đăng nhập tới, audit `reset_2fa`/`enable_2fa`). **Đã test E2E bằng script TOTP thật:** bật 2FA bằng mã → vào được app; phiên mới: mật khẩu → 302 `/login-2fa` → mã đúng → vào app; admin đã được **reset về chưa cài 2FA** (xóa key+recovery codes trong DB) để user tự quét bằng điện thoại thật. Build **0 warning/0 error**, web `http://localhost:5177` `/login` 200. **Chưa commit.** ⚠️ Từ giờ MỌI tài khoản (kể cả demo đối tác agent/ctv) sẽ bị bắt cài Authenticator ở lần đăng nhập tới.
## ⏭️ VIỆC TIẾP THEO (baton — làm cái này trước)

**#0X — DUYỆT MẮT CỔNG CÁ NHÂN HÓA PHỤ HUYNH/HỌC SINH (Session 60 — CODE XONG, BUILD 0/0, MIGRATION ĐÃ ÁP, WEB ĐANG CHẠY :5177, CHƯA commit):**
- **Tạo tài khoản test:** đăng nhập `admin@polymind.local`/`Admin@123` → mở 1 ứng viên ĐÃ đặt cọc + có đơn hàng (gợi ý có cả khoản vay để test đủ: **UV-20260608-2001 Lê Hữu Khang**). Ở đầu trang chi tiết thấy khu **"Tài khoản đăng nhập (cổng cá nhân hóa)"** 2 dòng Học sinh/Phụ huynh → bấm **Tạo** cả hai (email gợi ý `hocvien-...`/`phuhuynh-...`, mật khẩu `Admin@123`). ⚠️ Lần đầu đăng nhập 2 tài khoản này sẽ bị **ép cài 2FA** (enforcement toàn hệ thống) — cần app Authenticator.
- **Học sinh:** đăng nhập (điện thoại/≈390px) → landing **`/me`** đủ 4-5 thẻ; **bottom-nav** hiện dưới; menu KHÔNG có Lead/Jobs/Đại lý/Báo cáo/Hướng dẫn/Quản trị. `/candidates` nhảy thẳng chi tiết mình; `/training` thẳng chi tiết đào tạo; `/finance` chỉ tiến độ của mình; `/loans` chỉ khoản vay của mình. `/messages` chỉ thấy CTV+TVV+**phụ huynh**; `/ai` chỉ tab Hỏi đáp, hỏi "tôi còn nợ bao nhiêu/đang bước nào" trả lời đúng, hỏi ứng viên khác → từ chối.
- **Phụ huynh:** như trên nhưng `/messages` thấy CTV+TVV+**con/em** (tài khoản học sinh); KHÔNG có Đại lý & Hoa hồng.
- **Không hồi quy (admin/nhân sự):** `/candidates` có **cột Trạng thái** (Ứng viên/Học viên/Lao động); trên **laptop ≤1600px** cột Giới tính chỉ còn icon; trang **Đào tạo** giờ ~14 người; card Đào tạo ở chi tiết chỉ hiện với ứng viên đã đặt cọc.
- **Ghi âm tin nhắn:** `/messages` → mic → dừng → hiện **khung nghe lại** (bấm play) → **Gửi** hoặc **Bỏ** (không tự gửi, hết lỗi "not a typed array or blob").
- **Gemini trên Render:** vào Render → service web → **Environment** → thêm `Ai__Gemini__ApiKey` = key (và có thể `Ai__Gemini__Model=gemini-2.5-flash`) → **Deploy lại**. (Local đã có key trong appsettings.Development.json.)
- **Khi user duyệt xong → COMMIT gộp** (cân nhắc gộp cả Session 55–59).

**#0Y — DUYỆT MẮT TRANG "PHỤ HUYNH & HỌC SINH" (Session 59 — CODE XONG, BUILD 0/0, KHÔNG migration, WEB ĐANG CHẠY :5177, CHƯA commit):**
- Đăng nhập `admin@polymind.local` / `Admin@123`. Vào `/admin` → thấy nút **"Phụ huynh & Học sinh"** ở góc trên phải; tab **Tài khoản** KHÔNG còn nhóm/bảng phụ huynh/học viên và ô "Vai trò" khi tạo KHÔNG còn lựa chọn Phụ huynh/Học viên.
- Bấm nút → sang `/admin/parents-students`: form tạo chỉ có 2 vai trò **Phụ huynh** / **Học viên**; tạo thử 1 tài khoản mỗi loại → hiện đúng bảng theo vai trò. Cột "Chuyển vai trò" có **dropdown** (chỉ 2 vai trò parent/student); đổi role → phải hiện **hộp nhập lại mật khẩu** xác nhận. Có nút **Xóa** (super admin) → bấm phải hiện hộp **"Xóa tài khoản?"** xác nhận; xác nhận thì tài khoản biến mất khỏi bảng.
- Kiểm chốt chặn: ở trang Tài khoản không tạo được parent/student (dropdown vai trò không có); ở trang P&S dropdown chỉ có parent/student (không đổi ra role khác được), guard server-side nếu lách.
- **Khi user duyệt xong → COMMIT gộp** (cân nhắc gộp cả Session 55–58).

**#0Z — DUYỆT MẮT ĐỒNG BỘ TIỀN BẠC 3 TRANG (Session 58 — CODE XONG, BUILD 0/0, KHÔNG migration, CHƯA restart/commit):**
- Restart web để nạp code. Đăng nhập admin (hoặc `accountant@` cho Thu nợ).
- `/loans` → **Thêm khoản vay**, chọn 1 ứng viên đã gắn đơn: chọn loại **Ngân hàng** → phải **ẩn** "Trừ lương/Bắt đầu trừ lương/Tạo lịch trả góp", hiện alert "chỉ lưu thông tin". Chọn loại **Nợ công ty** → ô "Số tiền nợ" **tự điền = chi phí đơn hàng** (sửa được), hiện lại trừ lương + checkbox tạo lịch + caption "gốc + lãi".
- Nợ công ty: nhập lãi suất + thời hạn, bật **Tạo lịch trả góp** → lưu → `/debt-collection` thấy tổng phải thu = **gốc + lãi** (không chỉ gốc), chia đều theo kỳ.
- `/debt-collection` (`accountant@`): **CHỈ** thấy khoản **Nợ công ty**; khoản vay ngân hàng (kể cả có lịch cũ từ seeder) **KHÔNG** còn hiện. Tick "Thu kỳ tới" → trả đủ auto "Đã tất toán".
- `/finance`: **giữ nguyên** — vẫn thủ công, người nợ công ty KHÔNG bị tự đánh dấu "đã đóng".
- **Khi user duyệt xong → COMMIT gộp** (cân nhắc gộp cả Session 55/56/57).

**#0A — RESTART WEB + DUYỆT MẮT 8 GÓP Ý (Session 57 — CODE XONG, COMPILE 0/0, KHÔNG migration, CHƯA restart/commit):**
- **PHẢI restart web** (tắt host cũ đang khóa DLL rồi chạy lại) để nạp code + để `DemoDataSeeder` chạy chuẩn hóa: `cd src/Polymind.Web; $env:ASPNETCORE_ENVIRONMENT='Development'; dotnet run` (hoặc `demo-start.ps1`). Ctrl+F5 lấy CSS/JS mới.
- (1) `/messages` nút **mic** khi CHƯA cấp quyền → báo tiếng Việt rõ ("Bạn đã chặn quyền micro…"), không còn "Permission denied undefined".
- (2) `/candidates/{id}` chip cạnh mã: ứng viên đơn **du học** (StudyAbroad) từ B5 = **Học viên** (xanh, icon trường); đơn khác = **Lao động**; trước B5 = **Ứng viên**.
- (3) Menu **Đào tạo** → `/training` (danh sách + tiến độ 2 mảng + phiếu gần nhất) → bấm 1 học viên → `/training/{id}` xem **timeline theo tuần** (thêm "Báo cáo tuần" = `TrainingEvaluationDialog`, sửa tiến trình từng mảng). Thẻ Đào tạo ở `/candidates/{id}` giờ chỉ **tóm tắt** + nút "Xem chi tiết đào tạo".
- (4) `/loans` lọc **Nợ công ty** phải thấy hồ sơ nợ VG (note "Tỉnh … không thuộc diện… nợ công ty (nợ VG)"); `/debt-collection` (đăng nhập `accountant@`) thấy các kỳ, tick "Đã thu".
- (5) `/notifications` (admin) KHÔNG còn 500 trùng khóa; lead-care nhắc lại resurface hàng cũ.
- (6) `/agents` + `/my-commissions`: hoa hồng đại lý GIẢM còn ~5% cost (trước là ~100%); CTV share 30-40%. Dialog CTV Min/Max 30-40.
- (7) Menu trái KHÔNG còn "Thông báo" (chỉ chuông góc phải).
- (8) `/candidates` trên **laptop** (≤1600px): cột "Bước hiện tại" nhãn rút gọn, KHÔNG cuộn ngang; trên màn hình rộng (>1600px) vẫn nhãn đầy đủ.
- **Khi user duyệt xong → COMMIT gộp** (cân nhắc gộp cả Session 55/56).

**#0 — DUYỆT MẮT + COMMIT BIÊN BẢN 3/7 (Session 56 — CODE XONG HẾT, BUILD IN-PLACE 0/0, MIGRATION ĐÃ ÁP, WEB ĐANG CHẠY :5177, CHƯA commit):**
- Web đang chạy `http://localhost:5177` (đăng nhập `admin@polymind.local` / `Admin@123`; cần circuit MudBlazor + Ctrl+F5). Nếu tắt máy: chạy lại từ `src/Polymind.Web`: `$env:ASPNETCORE_ENVIRONMENT='Development'; dotnet bin\Debug\net10.0\Polymind.Web.dll --urls http://localhost:5177`.
- **1A Đào tạo:** `/candidates/{id}` thẻ **Đào tạo** (2 ô Học tiếng/Chuyên môn + %), nút **Phiếu đánh giá** (4 tiêu chí + đính kèm) → danh sách phiếu; `agent@`/CTV read-only.
- **1B Công nợ:** `/loans` tạo **Nợ công ty** + "Tạo lịch trả góp"; **Thu nợ** (`/debt-collection`, `accountant@`) tick kỳ đã thu → trả đủ auto **Đã tất toán** → mở B20.
- **1C Hoa hồng:** `/finance` tick 1 giai đoạn đóng tiền = Đã thu → sinh 1 lát hoa hồng (snackbar); kiểm `/my-commissions`.
- **2D–2H:** chip **Lao động** từ B5; đăng nhập CTV (`ctv-ctv0001@` / `Admin@123`) KHÔNG thấy bảng thi đua CTV + SĐT bị mask; `/agents/tree`; ô tìm `/jobs`; `/leads` tìm theo tỉnh/nước.
- **2I Ghi âm:** `/messages` → nút **mic** (cấp quyền micro) → ghi → dừng → chip "Ghi âm N giây" → **Gửi** → người nhận thấy trình phát `<audio>`. (Cần trình duyệt thật + micro; localhost OK cho getUserMedia.)
- **Tài khoản Học viên:** `/candidates/{id}` nút **"Tài khoản Học viên"** (admin) → tạo `hocvien-<mã>@polymind.local` / `Admin@123` role student → đăng nhập tài khoản đó phải CHỈ thấy đúng 1 hồ sơ của mình (`/candidates` lọc còn 1, dán URL hồ sơ khác bị chặn).
- **Phụ huynh:** role `parent` = quyền CTV; tạo qua `/admin` (chọn role Phụ huynh). Chưa có UI gắn con cho Phụ huynh (dùng chung `Candidate.OwnerUserId` — hiện ưu tiên Học viên; nếu cần Phụ huynh xem con thì set `OwnerUserId` = user phụ huynh).
- **RESET ACC CTV (đã xong):** 13 `ctv-*` mật khẩu `Admin@123` (verify Success). Các acc chưa bật 2FA → lần login tới bị ép cài Authenticator (enforcement bắt buộc). Muốn miễn 2FA cho partner/CTV demo phải nới `TwoFactorEnforcement`.
- **Khi user duyệt xong → COMMIT gộp** (cân nhắc gộp cả Session 55).

**#1 — COMMIT + DUYỆT MẮT MỤC 5 (Session 55 đã code xong, build xanh, CHƯA commit):** cân nhắc commit gộp các session gần đây khi user duyệt xong. Duyệt mắt bằng trình duyệt thật (`admin@polymind.local` / `Admin@123`, cần circuit MudBlazor + Ctrl+F5 lấy CSS mới):
- **Quy trình 20 bước** `/candidates/{id}`: stepper hiện đủ 20 bước (số 1–20), thẻ "Bước hiện tại" hiện đúng nhãn mới. Đưa 1 ứng viên tới **B8 (Thi tuyển)**: phải thấy radio chọn 1/3 tình huống + nút "Đậu" và nút "Rớt → về 7.5". Bấm **Rớt**: hồ sơ về **B7.5** (chip cam), phải bắt **gắn đơn hàng MỚI** (dropdown loại đơn cũ) mới sang B8; hồ sơ giữ nguyên. Kiểm **B10** (chọn nhiều nội dung học/Bỏ qua), **B15** (Thanh toán hoàn tất | Cam kết trả nợ — chọn "Cam kết trả nợ" mà chưa có hồ sơ vay thì bị chặn, phải tạo vay ở thẻ Hỗ trợ vay). Tới **B19**: thẻ **Nhật ký tương tác giai đoạn xứ người** (thêm được nhiều ghi chú); nút sang **B20** bị chặn nếu khoản vay chưa "Đã tất toán" — vào thẻ vay đổi trạng thái Settled rồi mới xác nhận hoàn thành.
- **Bảng thi đua `/agents`**: cột đổi tên **"Ứng viên đặt cọc"** — đếm ứng viên đạt bước Đặt cọc trong **tháng đang lọc**; đổi tháng lọc thấy số nhảy; tháng không có ai đặt cọc thì cột = 0.
- **Loans `/loans`**: có thẻ tổng quan **"Đã tất toán"** + trạng thái này trong bộ lọc và dialog thêm/sửa vay.

**#2 — SỬA GIAO DIỆN 2FA LỆCH TRÁI (Session 55, đã code + build xanh):** mở `/login-2fa` (sau khi nhập mật khẩu tài khoản đã bật 2FA) và `/account/2fa-setup` (tài khoản chưa cài) → thẻ trắng phải **canh GIỮA màn hình** trên PC/laptop/iPad/điện thoại (trước đây dính lề trái ở desktop). Nếu còn lệch do cache CSS cũ → Ctrl+F5 (đã cache-bust `login.css?v=20260703-2fa-center`).

**FILE PHẢN HỒI ĐỐI TÁC (Session 55):** `POLYMIND - Phan hoi gop y Vietgroup Edu.docx` ở root repo — bám đúng 4 mục góp ý gốc, mỗi ý nêu "Góp ý → Đã cập nhật gì". Gửi đối tác khi user duyệt.

**DUYỆT MẮT MỤC 1 BẢO MẬT (Session 54b — đã code xong, cần duyệt bằng điện thoại thật):** (1) Cài **Google/Microsoft Authenticator** trên điện thoại. (2) Đăng nhập `admin@polymind.local` / `Admin@123` → hệ thống tự ép sang `/account/2fa-setup` → quét QR bằng app → nhập mã 6 số → **lưu 8 mã khôi phục** (chỉ hiện 1 lần) → vào hệ thống. (3) Đăng xuất, đăng nhập lại: sau mật khẩu phải hiện trang **nhập mã 6 số**; thử nhập sai để thấy báo lỗi; nhập đúng để vào. (4) Vào `/admin` tab Tài khoản: hàng super admin hiện chip đỏ **"Cố định"** (không còn dropdown role + nút Lưu role); đổi role một tài khoản khác → phải hiện dialog **nhập lại mật khẩu của mình**; xác nhận **không còn nút "Reset mật khẩu"** ở mọi role. (5) Bấm **Sửa** một tài khoản đã bật 2FA → thấy nút **Đặt lại 2FA**. ⚠️ Mọi tài khoản demo (agent/ctv...) từ giờ cũng bị bắt cài 2FA ở lần đăng nhập tới — nếu demo cho đối tác thì báo trước họ cần app Authenticator. Nếu super admin duy nhất mất cả điện thoại + mã khôi phục → phải sửa DB tay (`update users set two_factor_enabled=false...`) — nên tạo super admin dự phòng và bật 2FA cho cả hai.

**GÓP Ý VIETGROUP ĐỢT 1 + PHẢN HỒI VÒNG 2 (Session 54):** đăng nhập admin (`admin@polymind.local` / `Admin@123`) kiểm: (1) `/leads` — lead đứng yên lâu có **icon chuông đỏ rung nhẹ** cạnh chip trạng thái (KHÔNG còn chữ "Cần chăm sóc..."), rê chuột hiện tooltip trạng thái + việc cần làm; (2) `/leads/{id}` lead quá hạn — banner cam/đỏ đầu trang; thử đặt **Lịch hẹn tư vấn** ngày/giờ đã qua → phải bị chặn (date picker khóa ngày cũ + báo lỗi khi lưu); (3) `/notifications` — thông báo **Nhắc chăm sóc lead** (job 5 phút/lần, đã xác nhận 71 bản ghi trong DB); (4) `/jobs` — subtitle chỉ "N jobs"; 2 bộ lọc **dropdown** Phân nhóm + Quốc gia (song song, item có icon nhóm/ảnh cờ) — kiểm cả mobile 390px; jobs tách section *trong nước → ngoài nước → du học*; **cờ là ảnh SVG thật** (wwwroot/img/flags, hết cảnh JP/TW chữ trên Windows); card hiện *Đăng dd/MM • Hạn dd/MM*, hạn ≤7 ngày **đỏ (còn N ngày)**; (5) **Thêm/Sửa Job** — Quốc gia là dropdown cờ cố định: Nhật Bản/Đài Loan/Hàn Quốc/Trung Quốc/Singapore/**Việt Nam** (thêm VN cho nhóm trong nước; job cũ nước lạ như Đức vẫn giữ được khi sửa); (6) vào ứng viên chưa gắn đơn → dropdown "Gắn vào đơn hàng" **không còn job hết hạn**, và có chặn cứng khi lưu. Còn chờ user chốt: mục 1 (2FA + xác nhận đổi quyền + lockout) và mục 5 (quy trình 20 bước) — câu hỏi đã gửi trong chat Session 54.

**NHẬN DIỆN TÀI KHOẢN ĐỐI TÁC:** đăng nhập `agent@polymind.local` / `Admin@123` và `ctv-ctv0001@polymind.local` / `Admin@123`. Kiểm `/candidates`: tiêu đề phải hiện đúng danh tính (**Ứng viên - <tên đại lý>** hoặc **Ứng viên - CTV <tên>**) và dữ liệu vẫn được bó hẹp đúng scope. Kiểm `/agents`: dòng của tài khoản hiện tại có nền nổi bật + chip **Tôi**; với CTV, dòng đại lý chủ quản có chip **Đại lý của tôi**; nếu không nằm trong Top 3/Top 5 vẫn phải được pin thêm với rank thật. Kiểm nhanh sáng/tối để màu highlight không chói.

**ICON MENU PARTNER:** đăng nhập tài khoản Đại lý/CTV và kiểm menu: **Bảng thi đua doanh số** phải dùng icon cúp/thành tích, **Hoa hồng của tôi** dùng icon tiền/paid, không còn trùng icon bắt tay. Nếu trình duyệt còn cache, Ctrl+F5.

**BẢNG THI ĐUA DOANH SỐ:** đăng xuất/đăng nhập lại tài khoản partner để nhận claim mới. Đăng nhập `agent@polymind.local` / `Admin@123` và `ctv-ctv0001@polymind.local` / `Admin@123`: menu phải hiện **Bảng thi đua doanh số**; `/agents` chỉ có 2 bảng thi đua đại lý/CTV + bộ lọc **Tháng thi đua**, không có nút **Thêm đại lý**, không có **Danh sách đại lý** phía dưới, bấm hàng đại lý không mở chi tiết. Tháng lớn hơn tháng hiện tại của năm hiện tại phải disabled. Dán `/agents/{id}` bằng partner phải tự quay về `/agents`. Với admin/nội bộ, menu vẫn là **Đại lý & Hoa hồng**, vẫn có danh sách đại lý và vào chi tiết bình thường.

**UI CHIP GIỚI TÍNH/CTV:** đăng nhập admin và kiểm nhanh `/leads`, `/candidates`: chip **Nữ** phải đỏ cả icon + viền, chip **Nam** phải xanh cả icon + viền. Vào `/agents/{id}`: danh sách CTV trực thuộc phải hiện chip xanh `CTV-<tên>` như trang `/agents`, bấm chip mở dialog thông tin CTV; nếu trình duyệt còn cache CSS cũ thì Ctrl+F5.

**TÁCH ROLE ĐẠI LÝ/CTV + TÀI KHOẢN ĐỐI TÁC:** đăng nhập thử 1 đại lý `agent@polymind.local` / `Admin@123` và 1 CTV mẫu như `ctv-ctv0001@polymind.local` / `Admin@123`. Kiểm `/my-commissions`: đại lý phải thấy tổng CTV thuộc mình, ứng viên thuộc đại lý, hoa hồng gross, phần chia CTV, phần còn lại và nút sửa `% hoa hồng CTV`; CTV phải thấy đại lý chủ quản, chỉ các ứng viên mình giới thiệu, hoa hồng qua đại lý và phần CTV được hưởng. Kiểm `/candidates`: đại lý chỉ thấy ứng viên của đại lý, CTV chỉ thấy ứng viên có `CollaboratorId` của mình; dán URL ứng viên của CTV khác phải bị chặn. DB hiện đã xác nhận `agents=5`, `agent role users=5`, `collaborators=13`, `collaborator role users=13`; web đang chạy `http://localhost:5177/login`.

**SỬA/XÓA DỮ LIỆU LÕI:** đăng nhập `document.staff@polymind.local` / `Admin@123` kiểm `/leads/{id}` có nút **Sửa thông tin** + **Xóa**, `/candidates/{id}` có **Sửa thông tin** + **Xóa**; bấm Xóa phải hiện hộp xác nhận. Đăng nhập `visa.staff@...` kiểm không thấy nút sửa/xóa thông tin cơ bản ứng viên (chỉ phần visa/flight theo quyền riêng). Đăng nhập Super Admin kiểm `/jobs/{id}` có **Sửa/Xóa Job**, `/loans` và card Hỗ trợ vay trong chi tiết ứng viên có **Sửa/Xóa khoản vay**; tất cả nút thao tác có hover nhấc/bóng nhẹ.

**UI NÚT ĐÍNH KÈM:** mở `/ai` → tab **Hỏi đáp** và hover nút kẹp giấy cạnh ô nhập: nút phải cân với ô nhập, có nền/viền/bóng đổi nhẹ và icon nhấc rõ. Kiểm thêm `/messages` khi hover nút đính kèm file; nếu trình duyệt còn cache CSS cũ thì Ctrl+F5.

**AUDIT LOG / GỬI FILE:** đăng nhập Super Admin (`admin@polymind.local` / `Admin@123`) mở `/admin` → tab **Nhật ký thao tác** để duyệt lại câu chữ tiếng Việt cho admin không chuyên. Mở `/messages`, chọn một tài khoản khác rồi thử gửi ảnh + PDF/Word/Excel; ảnh phải xem nhanh trong khung chat, file còn lại có nút tải xuống. Mở `/ai` → tab **Hỏi đáp**, gửi kèm ảnh/PDF/TXT/CSV để Gemini phân tích; nếu người dùng cần AI đọc Word/Excel thì làm bước chuyển đổi sang PDF hoặc mở rộng luồng convert file sau.

**WEB LOCAL / SMOKE UI:** trong Session 53, `http://localhost:5177/login` chưa lên vì Docker Desktop/Linux engine chưa chạy; `docker compose up -d` lỗi `dockerDesktopLinuxEngine ... The system cannot find the file specified`. Muốn kiểm UI: mở Docker Desktop trước, chạy `docker compose up -d`, rồi chạy web từ `src/Polymind.Web` bằng `dotnet bin\Debug\net10.0\Polymind.Web.dll --urls http://localhost:5177`; log ở `C:\tmp\polymind-web.out.log` / `C:\tmp\polymind-web.err.log`.

**QUYỀN / WORKFLOW / TÀI CHÍNH:** đăng nhập Giám đốc (`director@polymind.local` / `Admin@123`) mở `/finance` xác nhận chỉ xem, không có nút **Đánh dấu đã đóng** hoặc **Duyệt** khoản thu. Mở chi tiết ứng viên ở bước 1/2/3 bằng role không phụ trách bước đó: phải thấy bước hiện tại + “Tài khoản này chỉ được xem tiến trình...” thay vì thông báo hoàn tất. Ứng viên chưa tới **Đặt cọc** không được hiện bảng **Công nợ ứng viên** trong chi tiết; từ bước **Đặt cọc** trở đi mới hiện. Smoke thêm `/agents/{id}`: Giám đốc chỉ duyệt hoa hồng pending, Kế toán/Super Admin mới thấy nút **Đã chi**.

**TÀI CHÍNH / BẢNG / BÁO CÁO:** kiểm `/finance` không còn Hồ Thị Nga/Trần Minh Châu nếu họ vẫn ở bước Lead/Tư vấn/chưa tới **Đặt cọc** trong trang ứng viên. Dialog thêm khoản thu chỉ liệt kê ứng viên đã tới bước Đặt cọc. Kiểm bảng thi đua `/agents`: CTV hiển thị chip `CTV-<tên>` giống Lead, không gạch chân; màu bảng dark mode dịu hơn. Kiểm `/reports` biểu đồ “Tỉnh/thành: Lead và Tỷ lệ chuyển đổi”: rê vào điểm phải hiện tooltip chi tiết. Kiểm `/jobs`: chip Đức nhìn được trong dark mode; quốc gia mới thêm từ đơn hàng mới sẽ tự xuất hiện trong bộ lọc vì filter group theo `JobOrders.Country`.

**BẢNG / TIN NHẮN / CHI TIẾT ỨNG VIÊN:** mở các trang có bảng như `/leads`, `/candidates`, `/jobs`, `/finance`, `/loans`, `/reports` ở cả sáng/tối để duyệt mắt màu bảng mới. Màu dark đã giảm sọc xám chói; nếu muốn dịu hơn nữa thì chỉnh nhóm rule `.theme-dark .mud-table...` trong `wwwroot/app.css`. Vào chi tiết ứng viên có TVV/CTV để xác nhận tên không còn gạch chân nhưng vẫn bấm mở dialog. Vào `/messages` kiểm tra danh sách người nhận chỉ cao tối đa khoảng 6 tài khoản và có scrollbar khi nhiều hơn 6.

**HỖ TRỢ VAY:** mở `/loans` kiểm trang chỉ hiển thị hồ sơ vay đã tạo, không còn toàn bộ ứng viên. Bấm **Thêm khoản vay** để chọn ứng viên hiện có chưa có khoản vay; dialog chỉ còn tình trạng **Đang vay** / **Đã giải ngân**. Vào chi tiết một ứng viên chưa có vay để thấy nút **Thêm khoản vay** thay vì trạng thái “Chưa vay”.
**AI DARK MODE / CHUYỂN THEME:** mở `/ai`, bấm nút sáng/tối trên app bar để kiểm cảm giác chuyển màu. Khung AI ở dark mode đã đổi sang nền xanh than, giảm chói; toàn app có transition ~0.28s cho background/color/border/shadow. Nếu trình duyệt còn giữ CSS cũ, Ctrl+F5 hoặc mở lại link public.
**HƯỚNG DẪN THEO ROLE:** mở `/guide` bằng từng tài khoản role để duyệt mắt nội dung hiển thị đúng quyền. Đã kiểm bằng admin/recruiter/accountant/visa/agent và responsive 1440/820/390px không tràn ngang; nếu muốn sửa câu chữ chi tiết cho từng phòng ban thì chỉnh `Components/Pages/Guide/Guide.razor` trong các nhánh `BuildSections(role)`.
**TRỢ LÝ AI UI:** user mở `/ai` → tab Hỏi đáp có shell mới; click tab **Trích xuất CV / ảnh** phải thấy upload zone hiện đại, không còn nút native `Choose File / No file chosen`. Nếu trình duyệt còn cache CSS thì Ctrl+F5.

**LOGO POLYMIND:** đã đồng bộ ảnh mới vào asset web và cache-bust. Nếu máy nào vẫn thấy logo cũ, dùng Ctrl+F5 hoặc mở lại link public; nếu đổi logo tiếp thì thay `POLYMIND.png` rồi regenerate `wwwroot/img/logo-polymind.png` + `favicon.png`.

**SERVER ĐỐI TÁC ĐANG MỞ:** gửi link **https://cleaner-reporter-breakfast-korean.trycloudflare.com** cho đối tác; đăng nhập demo `admin@polymind.local` / `Admin@123`. Link Cloudflare Quick Tunnel đổi mỗi lần restart tunnel; nếu mất kết nối hoặc muốn đổi link thì chạy lại `scripts/demo-start.ps1`. Tắt demo: `scripts/demo-stop.ps1`.

**DUYỆT MẮT Session 34 (UX + AI + Tài chính tuần tự) — web đang chạy local `http://localhost:5177` và public tunnel ở trên, đăng nhập admin (`admin@polymind.local` / `Admin@123`):** (1) `/leads` → chip **`CTV-<tên>`** bấm ra dialog CTV, **rê chuột thấy chip nhấc lên/sáng** (báo bấm được); cột "Mã" hiện **`#XXXX`**. (2) `/candidates` → cột "Tư vấn viên" là **chip xanh `TVV-<tên>`** có hover, bấm ra dialog TVV. (3) `/ai` (giờ là **nút icon ✨ trên app bar**, cạnh nút sáng/tối — không còn trong menu trái) tab Hỏi-đáp → hỏi "ứng viên nào tiềm năng nhất?" → AI dùng **tên thật + số liệu**, không còn từ chối. (4) `/finance` tab "Tiến độ đóng tiền": chỉ **bước kế tiếp** có nút "Đánh dấu đã đóng", bước sau khóa "Chưa tới lượt" → **ép đóng tuần tự 1→4**; record lệch của Trần Minh Châu đã dọn (B4 về Chưa đóng). (5) Nút **sáng/tối** đã có sẵn. **Lưu ý:** chip TVV/CTV + nút stepper Tài chính là tương tác (MudDataGrid/MudTabs) → chỉ hiện đủ khi circuit kết nối (trình duyệt thật). AI gửi kèm ảnh chụp dữ liệu mỗi câu hỏi → tốn token; DB lớn thì giảm `Take(100)`. **Khi user OK → commit gộp** Session 27→34.



**DUYỆT MẮT Session 32 (5 yêu cầu mới) — web hiện đã chạy tại `http://localhost:5177`; user đăng nhập admin (`admin@polymind.local` / `Admin@123`) kiểm:** (1) `/candidates/{id}` → card **"Người phụ trách ứng viên"** trên cùng, bấm tên **TVV**/**CTV** ra dialog liên lạc; cuộn xuống cột trái có card **"Hỗ trợ vay vốn"** (bấm "Tạo/Cập nhật hồ sơ vay"); nút **"Phân tích AI"** ở đầu trang → dialog Gemini phân tích hồ sơ. (2) `/agents` chỉ còn **Top 3 đại lý + Top 5 CTV**. (3) `/admin` tab Tài khoản → nút **"Sửa"** (super admin) đổi họ tên/email/mật khẩu. (4) `/loans` (nav **"Hỗ trợ vay"**): 4 thẻ tổng quan + lọc tình trạng + bảng ứng viên. (5) `/ai` (nav **"Trợ lý AI"**): tab **Hỏi-đáp** (gõ câu hỏi XKLĐ) + tab **Trích xuất CV** (upload ảnh/PDF). **Lưu ý/quyết định cần user chốt:** (a) **Model Gemini = `gemini-2.5-flash`** vì key free user cấp **không có quota cho `gemini-2.0-flash` (limit 0)** — nếu user xin được key xịn/đổi project thì sửa `Ai:Gemini` trong `appsettings.Development.json`. (b) **Key đang để trong `appsettings.Development.json` (file ĐƯỢC track git)** → **KHÔNG push public** kẻo lộ key; production đọc qua env `Ai__Gemini__ApiKey`. (c) Card TVV/CTV + card vay + dialog AI + nút Sửa tài khoản là **tương tác (MudDialog/MudDataGrid)** → chỉ hiện đủ khi circuit kết nối (mở trình duyệt thật). (d) Module vay hiện 1 hồ sơ/ứng viên (upsert); muốn nhiều khoản vay/lịch sử thì mở rộng sau. **Khi user OK → commit gộp** Session 27→32.

**DUYỆT MẮT Session 31 (Báo cáo + Tài chính 4 bước + Giới tính) — user mở `:5177` đăng nhập admin kiểm:** `/reports` → đổi **"Khoảng thời gian"** (Tháng/Quý/Năm/Tùy chọn) thấy số liệu + biểu đồ cập nhật; cuộn xuống mục **"Biểu đồ trực quan"** xem đủ **7 loại** (tròn/cột/đường/miền/thanh ngang/phân tán/kết hợp). `/finance` tab **"Tiến độ đóng tiền"**: mỗi ứng viên có stepper 4 bước + % hoàn tất; thử **"Tạo lịch đóng tiền"** cho ứng viên chưa có lịch + **"Đánh dấu đã đóng"** 1 bước. `/candidates` + `/leads` có cột **"Giới tính" (Nam/Nữ)**. Lưu ý cột Giới tính (MudDataGrid) + tab Tài chính (MudTabs) chỉ hiện khi circuit kết nối — mở bằng trình duyệt thật. **Gap/quyết định cần user chốt:** (a) **chia 20/30/30/20 + tên B2/B3** ("Phí dịch vụ"/"Phí trước xuất cảnh") do mình đặt theo yêu cầu — đổi tỉ lệ thì sửa `Display/PaymentSchedule.cs`, đổi tên thì sửa `Labels.Vi(PaymentStage)`. (b) Bộ lọc thời gian KHÔNG áp cho biểu đồ "6 tháng gần nhất" theo cửa sổ trượt (vẫn lấy 6 tháng cuối) — nếu user muốn biểu đồ tháng bám đúng khoảng lọc thì chỉnh vòng lặp tháng trong `Reports.LoadData()`. (c) Stage payment seed cộng thêm vào KPI "Tổng đã thu" (song song payment demo cũ) → nếu thấy số to bất thường là do chồng, có thể TRUNCATE payments rồi để seed lại. **Khi user OK → commit gộp** Session 27→31.

**DUYỆT MẮT Session 30 (Tư vấn viên + CTV-nguồn) — user mở `:5177` đăng nhập admin kiểm:** `/candidates` (cột **"Tư vấn viên"** gạch chân, bấm ra dialog SĐT/email), `/leads` (cột Nguồn vài lead hiện **`CTV-<tên>`**), **Thêm Lead** → chọn Nguồn="Giới thiệu" hiện ô autocomplete CTV + dropdown "Tư vấn viên"; lưu xong cột Nguồn ra `CTV-<tên>`. Thử **đăng nhập 1 TVV** `tuvan1@polymind.local` / `Admin@123`. `/admin` có bảng vai trò "Tư vấn viên" (5 tài khoản). Cột "Tư vấn viên" trong DataGrid nằm `MudHidden` → chỉ hiện khi circuit kết nối (trình duyệt thật). **Gap/đề xuất nếu user muốn tiếp:** (a) CandidateDetail chưa hiển thị TVV (chỉ có ở list + dialog) — thêm 1 dòng nếu user cần. (b) Lead cũ assigned=admin (không phải consultant) → có thể chạy 1 lần gán lại TVV cho lead cũ nếu muốn đồng nhất. (c) cho TVV tự nhắn tin/cổng riêng nếu cần. **Khi user OK → commit gộp** Session 27→30.

**SAU CUỘC HỌP (Session 29) — user duyệt mắt trong trình duyệt rồi báo gap:** vào `:5177` đăng nhập admin, kiểm: `/agents` (2 bảng thi đua + bấm tên CTV ra dialog liên lạc), `/jobs` (chip cờ lọc nước + số tiền lấp lánh + bấm đơn → card Đãi ngộ&Thưởng), `/candidates` (cột CTV gạch chân bấm được + Quốc gia cờ + Nghề + sort), `/messages` (gửi thử tin). Lưu ý 2 bảng thi đua nằm trong `MudHidden` nên chỉ hiện khi circuit kết nối (mở bằng trình duyệt thật, không phải prerender). **Gap còn lại đã báo user:** (a) CTV/đại lý chưa có tài khoản đăng nhập riêng → nhắn tin hiện chỉ giữa **8 user role** (giám đốc/đại lý = role `director`/`agent`); muốn CTV tự đăng nhập & nhắn cần tạo tài khoản cho từng CTV (việc lớn). (b) "doanh số" bảng thi đua = tổng **tiền đã thu (Payment Paid)** của ứng viên thuộc đại lý/CTV — nếu họp muốn công thức khác (theo hoa hồng/đầu người) thì chỉnh `Agents.razor Load()`. (c) realtime nhắn tin chưa có (phải refresh/đổi người để thấy tin mới). Khi user OK → **commit gộp** Session 27→29.

**PASS 2 UI (Session 28) — đã xong trang chi tiết + bảng mobile; có thể tiếp PASS 3 nếu user muốn:** rà nốt các bảng `MudSimpleTable`/`MudDataGrid` chưa có thẻ mobile ở các trang còn lại (Reports, Notifications, MyCommissions...) — đa số đã xử lý ở Phase 3, nhưng nên QA mắt 390px. **QA pixel 390/820/1440** (nợ lâu nay) vẫn cần con người lướt theo `docs/08-qa-mobile-checklist.md`. Cân nhắc **commit gộp** Session 27+28 khi user duyệt giao diện.

**GIAI ĐOẠN TEST (hiện tại): laptop + Cloudflare Tunnel — user tự vận hành hằng ngày.**
- Mỗi ngày muốn cho 2 đối tác test: chạy **`scripts/demo-start.ps1`** (double-click hoặc PowerShell) → nó in + copy **link `*.trycloudflare.com`** → gửi đối tác. Tắt: `scripts/demo-stop.ps1`.
- Link đổi mỗi lần restart tunnel → gửi lại link mới. Giữ laptop cắm điện + không sleep khi đang test. Sửa code xong: chạy lại `demo-start.ps1` (hoặc `dotnet watch`).
- **Việc của AI session sau (nếu user yêu cầu):** cải tiến demo (vd link cố định nếu user mua domain rẻ; hoặc `dotnet watch` để hot-reload khi sửa theo góp ý đối tác). KHÔNG cần đụng deploy production tới khi web chốt xong.

**Khi web CHỐT XONG → dùng thật (để sau):** chọn Oracle Always Free (free 24/7, cần thẻ xác minh) hoặc VPS VN trả phí (data trong nước, hóa đơn VAT — trình sếp). Code production đã sẵn sàng từ Session 25 (seeding an toàn, profile Caddy/DuckDNS). Chi tiết bên dưới:

**HARDENING DEPLOY phần code đã XONG (Session 25). Khi deploy thật cần MÁY/MẠNG (không tự động được trên máy dev):**
1. **Tạo `.env.production` thật** từ `.env.production.example`: đổi MỌI secret (`POSTGRES_PASSWORD`, `MINIO_ROOT_PASSWORD`, `JWT_KEY` >= 32 ký tự), đặt `SUPERADMIN_EMAIL` + `SUPERADMIN_PASSWORD` mạnh (đây là tài khoản đăng nhập duy nhất ở production — KHÔNG còn `Admin@123`), đặt `DOMAIN`=`polymindolms.duckdns.org` (fallback `polymindolmsvn`/`polymindolms2026`) + `ACME_EMAIL`.
2. **Kiểm tra mạng (gate bắt buộc):** so WAN IP của router với public IP ngoài Internet (vd whatismyip). Nếu KHÁC nhau → CGNAT → DuckDNS public KHÔNG đủ, chuyển fallback tunnel (Cloudflare Tunnel). Nếu GIỐNG → port-forward 80/443 về máy chạy Docker, trỏ bản ghi A DuckDNS về public IP (IP động thì bật `--profile duckdns`).
3. **Chạy production:** `docker compose --env-file .env.production -f docker-compose.production.yml --profile caddy up -d --build`. Đợi Caddy xin cert Let's Encrypt (log `docker logs polymind-prod-caddy`), test `https://<DOMAIN>/health`, chạy `scripts/smoke-test.ps1` qua domain thật, đăng nhập bằng super admin vừa tạo.
4. **Backup hằng ngày:** lên lịch `scripts/backup.ps1 -EnvFile .env.production`.
5. **QA giao diện bằng mắt** (nợ từ Session 23, cần con người): 390/820/1440px theo checklist Phase 5.
- **Lưu ý kỹ thuật Phase 2+3+4 (cho người sau):**
  - Component dùng chung ở `Components/Shared/`: `StatCard.razor` (param `Title/Value/Icon/Color/Caption`, tự render `MudItem xs=6 sm=4 md=3 lg=2`), `PageHeader.razor` (param `Title/Subtitle` + slot `Actions`). Đã import sẵn trong `_Imports.razor` (`Polymind.Web.Components.Shared`).
  - Pattern bảng→thẻ: `<MudHidden Breakpoint="Breakpoint.SmAndDown">` bọc DataGrid (chỉ ≥md), `<MudHidden Breakpoint="Breakpoint.SmAndDown" Invert="true">` bọc danh sách thẻ (chỉ ≤sm). Mỗi trang tự render thẻ riêng (cột mỗi entity khác nhau).
  - CSS mới trong `wwwroot/app.css`: `.stat-value` (cỡ chữ KPI co theo breakpoint), `.mobile-card` (viền+bo góc thẻ mobile), `.table-scroll-x` (đã có sẵn — cuộn ngang bảng rộng).
  - **Phase 4:** dialog defaults đặt 1 chỗ ở `MainLayout.razor` (`<MudDialogProvider FullWidth MaxWidth=Medium CloseButton CloseOnEscapeKey>`) → áp dụng cho mọi `DialogService.ShowAsync`. MudTabs (9.5) tự hiện nút cuộn khi tab tràn → không cần thêm attribute (tránh bẫy MUD0002). Timeline dùng `TimelinePosition.Start` + form field `xs=12` đã responsive sẵn.
  - **Phase 1→5 + Phase H đều CHƯA commit.** Cân nhắc commit trước khi deploy.

**Việc khác (chưa ưu tiên — sau khi xong đại tu giao diện):** deploy production (xem memory deploy-plan). Backlog Phase H cũ:
1. **Mở rộng REST API** theo mẫu đã có (`src/Polymind.Web/Api/`): thêm CRUD/read cho payments, expenses, agents, visas, flights, reports... Mẫu: dùng `ApiAuth.Bearer("<resource>:<action>")` cho từng endpoint, DTO trong `ApiContracts.cs`, map trong `Program.cs`. (Đã có sẵn: auth, leads CRUD, candidates/job-orders read.)
2. **Hạng mục Phase H còn lại** (phụ thuộc provider/thiết kế riêng — chốt scope trước khi code): OCR CCCD/Passport, chữ ký số, Facebook/TikTok/Google/Zalo lead intake (giờ đã có nền REST + webhook), AI chatbot/dự đoán (cần Claude API key), mobile app, BI/Data Warehouse.
3. **Xác minh nốt nhắc hoa hồng (từ Session 21):** demo data 8 hoa hồng đều `Pending` nên nhắc `CommissionPayment` chưa hiện. Để thấy: vào `/agents/{id}` duyệt 1 hoa hồng (Approved) rồi "Quét nhắc việc" ở `/notifications`. Logic đã verify qua build + đối xứng code.

**Lưu ý REST API (Phase H) cho người sau:**
- Code API ở `src/Polymind.Web/Api/`: `JwtTokenService` (sinh JWT + claim `permission`/role), `ApiContracts` (DTO + helper `ApiAuth.Bearer`), `AuthEndpoints`, `LeadsEndpoints`, `ResourceEndpoints`. Map ở cuối `Program.cs`.
- **Khóa JWT:** dev đặt trong `appsettings.Development.json` (`Jwt:Key`); **production BẮT BUỘC** env `JWT_KEY` (đã thêm vào `docker-compose.production.yml` + `.env.production.example`) — Program.cs throw nếu thiếu key ở production.
- JWT chạy song song Cookie: endpoint API phải `.RequireAuthorization(ApiAuth.Bearer("res:action"))` để ép scheme Bearer (trả 401, không redirect cookie). Đừng đổi default scheme (Blazor cần Cookie).
- Swashbuckle 10.2.3 dùng **Microsoft.OpenApi 2.x**: types ở namespace `Microsoft.OpenApi` (KHÔNG phải `.Models`); security requirement dùng `OpenApiSecuritySchemeReference(id, doc, null)` qua overload `AddSecurityRequirement(doc => ...)`; value của requirement là `List<string>` (không phải mảng).
- JSON API set `JsonStringEnumConverter` toàn cục (enum đọc/ghi dạng chuỗi). PowerShell 5.1 test API tiếng Việt: phải gửi body bằng `[Text.Encoding]::UTF8.GetBytes(...)` + `charset=utf-8`, nếu không bị 400/mojibake.
2. Nếu chưa chốt Phase H, việc thực tế nên làm ngay là **deploy rehearsal**: tạo `.env.production`, đặt chứng chỉ thật vào `deploy/nginx/certs/`, chạy `docker compose --env-file .env.production -f docker-compose.production.yml up -d --build`, test `/health`, chạy `scripts/smoke-test.ps1` qua domain thật.
3. Commit/push các thay đổi Phase F→G lên GitHub nếu muốn đồng bộ remote trước khi triển khai.
4. Nếu chỉ cần chạy demo local: `docker compose up -d`, rồi trong terminal chạy DLL build sẵn từ `src/Polymind.Web`: `$env:ASPNETCORE_ENVIRONMENT='Development'; dotnet bin\Debug\net10.0\Polymind.Web.dll --urls http://localhost:5177`.

**Quyết định deploy nội bộ đã chốt với user (Session 24, chỉ ghi kế hoạch — chưa code):**
- Hosting: **máy Windows công ty hiện có**, uptime kỳ vọng **giờ hành chính**.
- Access model: **link public** để nhân viên chỉ cần mở URL và login, không bắt cài VPN/Tailscale giai đoạn đầu.
- DNS free: **DuckDNS**, ưu tiên `polymindolms.duckdns.org`; fallback tên nếu bận: `polymindolmsvn.duckdns.org`, `polymindolms2026.duckdns.org`.
- Dữ liệu production: **DB sạch**, không seed demo data.
- Security gate bắt buộc trước khi public: không để tài khoản mẫu/mật khẩu `Admin@123` tồn tại/dùng được ở production; tạo user thật, mật khẩu mạnh, backup trước khi mở port.
- Network gate bắt buộc: kiểm tra router WAN IP so với public IP ngoài Internet. Nếu khác nhau/CGNAT hoặc không port-forward được 80/443 thì DuckDNS public không đủ; chuyển fallback tunnel.
- HTTPS/proxy đề xuất: thêm **Caddy** cho profile DuckDNS để tự xin/gia hạn Let's Encrypt cert; giữ Nginx hiện tại (`deploy/nginx/default.conf`) làm fallback/legacy.
- Fallback: Cloudflare Quick Tunnel chỉ dùng test/dev; ngrok free chỉ dùng pilot ngắn vì có giới hạn data/request và interstitial; nếu muốn ổn định hơn mà không mở port được thì cân nhắc mua domain rẻ + Cloudflare Tunnel/Access.

**Lưu ý Đợt 5 cho người sau:**
- `AgentScope` (scoped, `Identity/AgentScope.cs`) là nguồn sự thật cho data-scope đại lý: `GetAsync()` trả `(IsAgentOnly, AgentId)`, cache trong 1 request. "Agent-only" = có role `agent` và KHÔNG kèm role nội bộ nào. Dùng nó ở mọi trang dùng chung cần bó hẹp.
- Đại lý gắn user qua `Agent.UserId` (seed ở `SeedExtrasAsync`, idempotent — chỉ gắn nếu chưa có agent nào trỏ tới user đó). Demo: `agent@` ↔ AG-000001.
- Redirect đại lý khỏi `/` và `/reports` đặt trong `OnInitializedAsync` trước khi load DB (defense-in-depth), cộng ẩn menu trong `NavMenu` (`_isAgentOnly`). Nếu thêm trang nhạy cảm mới, nhớ cả 2 lớp.
- Permission seed cho `agent` đã được reconcile ở Session 18: hiện chỉ còn `candidates:read`, `commissions:read`, `notifications:read`; vẫn giữ redirect/ẩn menu như lớp bảo vệ phụ.

**Lưu ý Đợt 4 cho người sau:**
- Trang `/notifications`: class page `Notifications` **trùng tên** property inject → đã đổi tên service inject thành `NotificationSvc` (đừng đặt lại thành `Notifications`).
- Reminder hiện sinh bằng Hangfire recurring job và nút "Quét nhắc việc" ở `/notifications`; topbar bell chỉ đếm unread. Idempotent theo `(UserId, Type, ReferenceId, Channel)` và nhắm recipient theo người phụ trách/fallback role.
- Export CSV là **minimal-API endpoint** trong `Program.cs` (`MapCsvExportEndpoints`), gated `.RequireAuthorization("reports:read")` qua policy provider động. Dùng BOM UTF-8 (`Encoding.UTF8.GetPreamble()`) để Excel đọc đúng tiếng Việt. Không ghi file ra repo — stream `Results.File`.
- MudIcon render ra SVG path (không có tên icon trong HTML) và MudMenu render lazy → grep HTML tìm tên icon/menu sẽ "miss"; verify bằng HTTP 200 + vắng `blazor-error-boundary` + query DB.

**Nợ nhỏ phát hiện ở Session 9:** role `agent` từng thấy `/reports` hơi rộng; Session 18 đã gỡ `reports:read`/`dashboard:read` bằng seeder reconcile. Vẫn cần dùng `AgentScope` cho mọi trang dữ liệu dùng chung mới.

**LƯU Ý khi tạo trang mới (giữ nguyên):** KHÔNG thêm `@rendermode`; đặt page trong folder + đừng đặt tên class trùng entity. MudBlazor 9.5 — tránh `MudChart` (API đổi, cảnh báo `MUD0002`, attribute bị nuốt im lặng); dùng `MudProgressLinear` + `MudSimpleTable`. **Seed mở rộng:** `DemoDataSeeder.SeedExtrasAsync` chạy idempotent theo từng bảng kể cả khi DB đã có Lead (bù Visa/Flight/Commission mà không cần xóa DB).

## 🚧 BLOCKERS / NỢ KỸ THUẬT

- **Smoke UI local Session 53:** Docker Desktop/Linux engine chưa chạy; `docker compose up -d` lỗi `dockerDesktopLinuxEngine ... The system cannot find the file specified`, nên chưa mở được `http://localhost:5177/login` để click kiểm tài khoản Đại lý/CTV. Code đã build xanh; bật Docker Desktop rồi smoke lại.
- Nợ triển khai thật: cần `.env.production` với secret thật, chứng chỉ TLS thật (ưu tiên Caddy tự cấp Let's Encrypt cho DuckDNS; Nginx manual cert vẫn là fallback), SMTP/provider SMS/Zalo thật nếu bật gửi ngoài InApp.
- **CẢNH BÁO DEPLOY PUBLIC:** Không public production nếu còn user mẫu/mật khẩu `Admin@123`. DuckDNS/No-IP chỉ giải quyết DNS động; vẫn cần public IP hoặc port-forward 80/443 hoạt động. Nếu mạng công ty bị CGNAT/không mở port được thì không cố DuckDNS, chuyển fallback tunnel.
- ~~Module placeholder chờ làm thật~~ — ĐÃ XONG HẾT (finance/agents/visa/reports đều có trang thật).

---

## 📜 NHẬT KÝ SESSION (mới nhất ở trên)

### [2026-07-09] Session 60 — Claude — Cổng cá nhân hóa Phụ huynh/Học sinh (mobile-first) + tài khoản Đại lý/CTV + sửa ghi âm + cột Trạng thái ứng viên
- **Yêu cầu user (nhiều đợt trong 1 phiên):** 2 role Phụ huynh/Học sinh phải thành cổng khách hàng cá nhân hóa (ưu tiên điện thoại): xem thẳng hồ sơ của mình thay vì bảng, có Tổng quan/Tài chính/Hỗ trợ vay riêng, Tin nhắn giới hạn đúng người liên quan, AI chỉ hỏi-đáp về mình; 1 ứng viên gắn 3 tài khoản (CTV/TVV + Học sinh + **Phụ huynh mới**). Phát sinh: sửa lỗi ghi âm tin nhắn (không tự gửi, nghe lại trước khi gửi); thêm cột Trạng thái (Ứng viên/Học viên/Lao động) + laptop ẩn chữ giới tính; Đào tạo mở cho mọi ứng viên đã đặt cọc; nút tạo tài khoản Đại lý/CTV ở `/agents/{id}` (đã có thì disable); Gemini lỗi key trên Render.
- **Làm được:** (chi tiết đầy đủ ở TRẠNG THÁI HIỆN TẠI mục Session 60). Tóm tắt: migration `AddCandidateParentUser`; `AgentScope`+`DbSeeder` (parent bỏ agents/commissions); `NavMenu`+`PortalBottomNav`+`RedirectToLogin`; trang `/me` (`Portal/Overview.razor`); cá nhân hóa Candidates/Training/Finance/Loans (nhánh self-scoped, guard `OwnedCandidateId`); tin nhắn theo quan hệ (`Messages.BuildAllowedRecipientsAsync`); AI giới hạn (`AiAssistant` restricted prompt + own-data); `ParentAccountDialog`+`PartnerAccountDialog`; ghi âm trả **Blob** + khung nghe lại; cột Trạng thái + `laptop-hide-inline` cho giới tính; `PersonTitle.IsTitled` mở rộng Training.
- **File thay đổi chính:** `Domain/Entities/Candidate.cs` + migration `*_AddCandidateParentUser`, `Infrastructure/Persistence/{ApplicationDbContext,DbSeeder}.cs`, `Web/Identity/AgentScope.cs`, `Web/Components/Layout/{NavMenu,MainLayout,PortalBottomNav(mới)}.razor`, `Web/Components/RedirectToLogin.razor`, `Web/Components/Pages/{Portal/Overview(mới),Candidates/{Candidates,CandidateDetail,ParentAccountDialog(mới)},Training/{Training,TrainingDetail},Finance/Finance,Loans/Loans,Messages/Messages,Ai/AiAssistant,Agents/{AgentDetail,PartnerAccountDialog(mới)}}.razor`, `Web/wwwroot/js/audio-recorder.js`, `Web/wwwroot/app.css`, `Web/Ai/GeminiClient.cs`.
- **Đã test:** `dotnet build src/Polymind.Web` = **0 warning/0 error** (build nhiều lần trong lúc code). Migration đã áp (DB có cột `parent_user_id`). Web CHẠY `http://localhost:5177` `/login` **200**, không lỗi khởi động. DB verify: quyền parent/student = candidates/training/payments/loans/notifications:read + messaging (parent hết agents/commissions); 14 ứng viên đã đặt cọc (Training sẽ hiện ~14); **5/5 đại lý + 13/13 CTV đã có tài khoản** → nút tạo tài khoản disable đúng. **Chưa duyệt mắt trình duyệt thật** (đặc biệt bottom-nav mobile + ghi âm cần micro + đăng nhập parent/student bị ép 2FA). **Chưa commit.**
- **Lưu ý/cảnh báo cho người sau:** parent/student đăng nhập lần đầu bị **ép cài 2FA** (enforcement toàn hệ thống) — báo user cần Authenticator khi test. AI self-scoped nạp hồ sơ CHÍNH ứng viên + prompt cấm lộ dữ liệu khác — nếu đổi model/prompt giữ nguyên ràng buộc này. **Key Gemini production KHÔNG commit** — phải đặt env `Ai__Gemini__ApiKey` ở dashboard Render. Ghi âm nay trả **Blob** qua `IJSStreamReference` (ổn định hơn Uint8Array) + data URL để nghe lại (clip ngắn, base64 trong DOM). IDE auto-format có thể hoàn tác edit → Read lại trước mỗi Edit.
- **[[project-polymind-olms]]** · **[[project-polymind-gemini-ai]]**

### [2026-07-07] Session 59 — Claude — Trang riêng "Phụ huynh & Học sinh" (tách khỏi trang Tài khoản, vai trò cố định)
- **Yêu cầu user:** ở Quản trị hệ thống thêm nút **"Phụ huynh & Học sinh"** → trang riêng **giống y chang** trang Tài khoản nhưng CHỈ quản lý tài khoản phụ huynh/học viên. Trang Tài khoản **không tạo được** role parent/student; trang Phụ huynh & Học sinh **chỉ tạo được** parent/student (không role khác). Role phụ huynh/học sinh **cố định**, không đổi sang role khác.
- **Làm được:** (1) Tách toàn bộ phần quản lý tài khoản (form tạo + bảng theo vai trò + tạo/sửa/khóa/đổi role) thành component dùng chung **`AccountManagerPanel.razor`** (tham số `ManagedRoles` + `RolesFixed`) → 2 trang chắc chắn giống hệt nhau, không lệch. Panel chỉ tạo/hiển thị/đổi trong phạm vi `ManagedRoles` (chốt chặn cả ở `CreateUserAsync`/`SaveUserRoleAsync`); khi `RolesFixed=true` mọi hàng hiện chip **"Cố định"** (như super admin) — ẩn dropdown role + nút "Lưu role", chỉ còn Sửa/Khóa. (2) `Admin.razor` tab Tài khoản nay dùng `<AccountManagerPanel ManagedRoles="@StaffRoles" />` (StaffRoles = mọi role trừ parent/student) + nút **"Phụ huynh & Học sinh"** ở PageHeader → `/admin/parents-students`; đã xóa code account cũ (LoadUsers/CreateUser/SaveRole/Toggle/RoleOrder/GroupedUsers/UserRow) khỏi Admin.razor (giữ tab Phân quyền + Nhật ký). (3) Trang mới **`ParentStudentAccounts.razor`** `/admin/parents-students` (gate `users:read`) = `<AccountManagerPanel ManagedRoles="{parent,student}" RolesFixed="true" />` + nút quay về Quản trị.
- **File thay đổi chính:** `Web/Components/Pages/Admin/AccountManagerPanel.razor (mới)`, `Web/Components/Pages/Admin/ParentStudentAccounts.razor (mới)`, `Web/Components/Pages/Admin/Admin.razor`.
- **Đã test:** `dotnet build src/Polymind.Web` sau khi dừng host cũ = **0 warning / 0 error**. Restart web, `/login` = **200**. **Không migration** (chỉ UI). **Chưa duyệt mắt trình duyệt thật** + **chưa commit**.
- **Lưu ý/cảnh báo cho người sau:** IDE (VS/OmniSharp) đang mở file có auto-format **hoàn tác một phần edit giữa chừng** → nếu sửa Admin.razor tiếp phải Read lại trước mỗi Edit. Trang này chỉ quản lý **tài khoản** (tạo user + gán role) — việc **gắn học viên với hồ sơ ứng viên** vẫn qua `StudentAccountDialog`/`Candidate.OwnerUserId` ở CandidateDetail (không đổi). `RolesFixed` chặn đổi role kể cả giữa parent↔student (đúng "cố định"); muốn cho đổi qua lại thì bỏ `RolesFixed` cho panel P&S.

### [2026-07-07] Session 58 — Claude — Đồng bộ logic tiền bạc 3 trang: Thu nợ chỉ nợ công ty, vay NH chỉ lưu thông tin, nợ công ty prefill chi phí đơn + tính gốc+lãi
- **Bối cảnh (user làm rõ):** 3 trang Tài chính / Hỗ trợ vay / Thu nợ chưa khớp nhau. Luồng đúng: người vay **ngân hàng** đem tiền NH giải ngân đóng các bước bên **Tài chính** → không nợ công ty → Thu nợ **vô nghĩa** với họ. Trang **Thu nợ chỉ dành cho người vay TIỀN CÔNG TY** (tỉnh không cho NH vay → công ty cho vay để họ đóng tài chính; XKLĐ xong trả lại **gốc + lãi**). Tài chính **giữ nguyên & thủ công** (công ty ứng tiền KHÔNG tự đánh dấu "đã đóng").
- **Làm được:** (1) `/debt-collection` (`DebtCollection.razor`) lọc `Kind==LoanKind.Company` — vay NH (kể cả có `LoanRepayment` cũ từ seeder) không còn hiện; đổi tiêu đề "Thu nợ công ty" + alert giải thích scope. (2) `LoanDialog.razor`: đổi select loại nợ sang `Value/ValueChanged=OnKindChanged`; khi **Ngân hàng** → ẩn "Trừ lương/Bắt đầu trừ lương/Tạo lịch trả góp" + hiện alert "chỉ lưu thông tin"; khi **Nợ công ty** → hiện lại + prefill `_amount = chi phí đơn hàng` ứng viên đang gắn (`GetOrderCostAsync` lấy `JobOrder.CostAmount` của `CandidateJobOrder` mới nhất), cho sửa. (3) `RegenerateScheduleAsync` nay chia **gốc + lãi đơn** (`interest = principal × rate/100 × term/12`, làm tròn), tổng chia đều theo kỳ (trước chỉ chia gốc). (4) `SaveAsync`: bank → set `MonthlyDeductionAmount/DeductionStartDate=null` + xóa `LoanRepayment` cũ; chỉ company mới sinh lịch (`_generateSchedule && Kind==Company`).
- **File thay đổi chính:** `Web/Components/Pages/Loans/DebtCollection.razor`, `Web/Components/Pages/Loans/LoanDialog.razor`, `WORKLOG.md`.
- **Đã test:** `dotnet build Polymind.slnx` = **0 warning / 0 error**. **Không migration** (chỉ đổi logic UI + query, không đổi schema). **Chưa restart/duyệt mắt trình duyệt** (web cũ đang chạy — cần restart để nạp code).
- **Lưu ý/cảnh báo cho người sau:** **CHƯA commit.** Công thức lãi hiện là **lãi đơn phẳng** (flat) chia đều — nếu Vietgroup muốn lãi giảm dần theo dư nợ thì sửa `RegenerateScheduleAsync` trong `LoanDialog.razor`. Khoản vay NH cũ trong DB có thể còn `LoanRepayment` mồ côi (không hiện ở Thu nợ do đã lọc); mở & lưu lại hồ sơ đó sẽ tự dọn. Trang **Hỗ trợ vay `/loans`** vẫn hiện CẢ 2 loại (bank + company) — đúng chủ ý (đây là sổ theo dõi mọi nguồn vốn).

### [2026-07-06] Session 57 — Claude — 8 góp ý duyệt mắt: sửa lỗi micro/thông báo, chip Học viên/Lao động theo phân nhóm job, MODULE ĐÀO TẠO riêng, nợ VG, hoa hồng 5%/CTV 35%, bỏ menu Thông báo, sửa tràn bảng laptop
- **Làm được (8 mục theo phản hồi user):**
  1. **Lỗi micro** (`audio-recorder.js`): `start()` kiểm tra `navigator.mediaDevices` + bắt lỗi getUserMedia → ném thông điệp tiếng Việt rõ (NotAllowed/NotFound/NotReadable); `Messages.razor` bắt `JSException` hiện thẳng message (hết "Permission denied undefined").
  2. **Chip Học viên/Lao động theo phân nhóm job** (`Display/PersonTitle.cs`): giữ ngưỡng B5, nhưng đơn **StudyAbroad → "Học viên"** (chip Info, icon School), nhóm khác → **"Lao động"**; CandidateDetail nạp thêm `_jobCategory`.
  3. **MODULE ĐÀO TẠO riêng:** trang `/training` (`Training.razor` — danh sách học viên/lao động đang đào tạo + tiến độ 2 mảng + phiếu gần nhất) và `/training/{id}` (`TrainingDetail.razor` — 2 mảng + **timeline theo TỪNG TUẦN** gom `TrainingEvaluation` theo tuần (Thứ 2 đầu tuần) + nút thêm báo cáo tuần/sửa tiến trình). NavMenu thêm **"Đào tạo"** (gate `training:read`). Thẻ Đào tạo ở `CandidateDetail` rút gọn còn **tóm tắt** (2 progress + báo cáo gần nhất) + nút **"Xem chi tiết đào tạo"** → module (bỏ nút quản lý khỏi thẻ, xóa `_canManageTraining`/`OpenTrackEdit`/`OpenAddEvaluation`).
  4. **Nợ VG** (`DemoDataSeeder`): block idempotent tạo vài **Loan `Kind=Company`** (tỉnh không được NH hỗ trợ, VG cho đi → nợ công ty) + lịch `LoanRepayment` (vài kỳ đã thu) → hiện ở `/loans` (Nợ công ty) + `/debt-collection`.
  5. **Lỗi thông báo trùng khóa** (`NotificationService.PersistEventsAsync`): lead-care "nhắc lại" trước đây CHÈN hàng mới → vỡ unique `ix_notifications_user_id_type_reference_id_channel` (23505). Nay **CẬP NHẬT hàng cũ** (reset chưa đọc + làm mới nội dung/thời điểm) thay vì chèn; thêm try/catch `DbUpdateException` phòng chèn song song.
  6. **Hoa hồng** (`Domain/Commissions/AgentCommissionRates.cs` mới): đại lý **TỔNG 5%** (1%/1.5%/2.5% theo mốc) số tiền ứng viên đóng; **CTV 35%** (khoảng 30-40%). Seeder set giá trị mới + **chuẩn hóa dữ liệu cũ** (config >5% → 1/1.5/2.5 + tính lại `AgentCommission.CommissionAmount`; CTV share kẹp về 30-40%). Dialog CTV Min/Max 30-40, config đại lý có HelperText.
  7. **Bỏ menu "Thông báo"** khỏi NavMenu (giữ route + icon chuông góc phải).
  8. **Tràn bảng laptop** (`/candidates`) — **ĐÃ ĐỔI HƯỚNG theo phản hồi:** giữ NGUYÊN nhãn "Bước hiện tại" ĐẦY ĐỦ (bỏ `ShortLabel`, `StepChip` về bản gốc); thay vào đó ≤1600px: cột **Quốc gia chỉ còn CỜ** (`.laptop-hide-inline` ẩn tên nước) + thanh **tiến độ ngắn lại** (`.candidate-progress-cell` 120→76px) + chip bước cho xuống dòng (`.candidate-table .step-chip white-space:normal`). PC >1600px y nguyên.
  9. **Thu nợ `/debt-collection` — tóm tắt hơn:** thẻ nợ nay hiện tóm tắt (còn nợ + progress + "K/N kỳ" + "Kỳ tới …") + nút **"Thu kỳ tới"** (thu nhanh kỳ chưa trả sớm nhất) + nút **"Xem lịch"** bung `MudCollapse` bảng đầy đủ (trước đây in thẳng 18-36 dòng/ứng viên → quá dài).
  10. **Micro** (từ phản hồi): thông điệp lỗi đã rõ; hướng dẫn user bật quyền micro trên trình duyệt. **Nguyên nhân THẬT** micro không chạy: header `Permissions-Policy` ở `Program.cs:199` để `microphone=()` (chặn) → đổi thành **`microphone=(self)`**. (Lỗi kế tiếp `NotFoundError` = máy user không có thiết bị micro — xử lý ở Windows, không phải code.)
  11. **Giới tính thứ 3** (biên bản họp 3/7): đổi nhãn `Gender.Other` "Khác" → **"Không đề cập"** (`Labels.cs`) — tùy chọn đã có sẵn trong form ứng viên.
  12. **File .docx** `POLYMIND - Da lam theo bien ban hop 3-7-2026.docx` (root, tạo bằng python-docx): ghi lại từng góp ý trong `POLYMIND HỌP 3_7_2026.pdf` + việc đã làm tương ứng, không thêm/bớt ý. App Demo (mục 4) ghi "ngoài phạm vi web"; "gửi data" ghi "chờ VG".
- **File thay đổi chính:** `wwwroot/js/audio-recorder.js`, `wwwroot/app.css`, `Components/Pages/Messages/Messages.razor`, `Components/Shared/StepChip.razor`, `Display/{PersonTitle,WorkflowSteps}.cs`, `Components/Pages/Training/{Training,TrainingDetail}.razor (mới)`, `Components/Layout/NavMenu.razor`, `Components/Pages/Candidates/{CandidateDetail,Candidates}.razor`, `Domain/Commissions/AgentCommissionRates.cs (mới)`, `Domain/Entities/Collaborator.cs`, `Infrastructure/Persistence/DemoDataSeeder.cs`, `Web/Notifications/NotificationService.cs`, `Components/Pages/Agents/{CollaboratorDialog,CommissionConfigDialog}.razor`.
- **Đã test:** `dotnet build -t:Compile` toàn solution = **0 warning / 0 error**. Full build chỉ vướng khóa DLL do web đang chạy (PID host) — **KHÔNG có lỗi code**. **Chưa migration** (toàn thay đổi code + seeder, không đổi schema). **Chưa chạy/duyệt mắt trình duyệt** (web cũ đang chạy — cần restart để nạp thay đổi + seeder chuẩn hóa hoa hồng/nợ VG).
- **Lưu ý/cảnh báo cho người sau:** **CHƯA commit.** Cần **restart web** để: (a) áp code mới, (b) DemoDataSeeder chạy chuẩn hóa hoa hồng cũ (20/30/50→5%) + kẹp CTV 30-40% + seed nợ VG. Số hoa hồng đại lý sẽ GIẢM mạnh (đúng ý). Chip Học viên/Lao động vẫn dùng ngưỡng B5 (trước B5 = "Ứng viên") — nếu user muốn gắn chip ngay khi vào job (không cần B5) thì bỏ `IsTitled` gate trong `PersonTitle`. Module Đào tạo "đang đào tạo" = có TrainingRecord/đánh giá hoặc đang ở B10. CSS bảng laptop cache — Ctrl+F5.
- **[[project-polymind-deploy-plan]]**

### [2026-07-06] Session 56 — Claude — Biên bản họp Vietgroup 3/7: Đợt 1 (3 module) + Đợt 2 (đủ 2D–2I) + tài khoản Học viên + reset acc CTV
- **Làm được:** Theo `POLYMIND HỌP 3_7_2026.pdf` + plan đã duyệt (`~/.claude/plans/polymind-h-p-3-7-2026...md`). **ĐỢT 1 — 1A Đào tạo:** enum `TrainingTrack{Language,Vocational}`/`EvaluationRating`; entity `TrainingRecord`+`TrainingEvaluation` (4 tiêu chí + `AttachmentsJson`); resource quyền `training` (director/recruitment_manager/consultant CRUD, agent/collaborator/parent read); thẻ **Đào tạo** ở `CandidateDetail` (2 ô Học tiếng/Chuyên môn + %), dialog `TrainingEvaluationDialog` (đính kèm qua `IDocumentStorage.UploadAsync`)+`TrainingTrackDialog`. **1B Công nợ:** `LoanKind{Bank,Company}`+`MonthlyDeductionAmount`/`DeductionStartDate`; entity `LoanRepayment`+`LoanRepaymentStatus`; `LoanDialog` chọn loại nợ + sinh lịch trả góp (`RegenerateScheduleAsync`); `Loans` thẻ "Nợ công ty"+lọc loại nợ+cột; **trang `/debt-collection`** (Kế toán thu từng kỳ → trả đủ auto `LoanStatus.Settled`) + NavMenu "Thu nợ" (gate `loans:update`). **1C Hoa hồng theo giai đoạn:** `Commissions/CommissionEngine.cs` (mốc↔PaymentStage: Deposit/ServiceFee/Settlement) sinh hoa hồng khi giai đoạn Paid, giữ NGUYÊN công thức số tiền cũ; thêm `AgentCommission.Stage`; gọi ở `Finance.MarkStagePaid`+workflow advance; **xóa** `EnsureCommissionsAsync`+`CommissionMilestones` cũ ở CandidateDetail. **ĐỢT 2:** 2D `Display/PersonTitle.cs` → chip **"Lao động"** từ B5 ở CandidateDetail; 2F mask SĐT khi CTV (`CandidateDetail.MaskPhone`+`MyCommissions.MaskPhone`) + ẩn bảng thi đua CTV (`@if(!_isCollaboratorOnly)` ở Agents); 2G role **Phụ huynh**(=CTV)+**Học viên**(self-only) trong `RoleNames`/`DbSeeder`, `Candidate.OwnerUserId`, `AgentScope.IsSelfScoped/OwnedCandidateId`, gate Candidates/CandidateDetail; 2H ô tìm ở `/jobs` (`MatchesSearch`: code/company/country/field) + `/leads` tìm thêm Province/TargetCountry; 2E trang `/agents/tree` (Công ty→Đại lý→CTV, custom nested); **2I ghi âm tin nhắn** (`wwwroot/js/audio-recorder.js` MediaRecorder → `IJSStreamReference`; `IDocumentStorage.UploadMessageAudioAsync(bytes)` + whitelist audio; nút mic + `<audio>` player ở `Messages.razor`); **UI tài khoản Học viên** (`StudentAccountDialog` tạo `hocvien-<mã>@` role student + set `Candidate.OwnerUserId`, nút ở CandidateDetail gate `users:create`). **3 migration** (design-time factory `ApplicationDbContextFactory` để `dotnet ef` chỉ build Infra, tránh khóa Web): `AddTrainingAndLoanRepayment` (sửa tay default `kind='Bank'`), `AddCommissionStage`, `AddCandidateOwnerUser`. **RESET ACC CTV:** reset 13 `ctv-*` về `Admin@123` (SQL update `password_hash` = hash Identity V3 tính bằng console `PasswordHasher`, + `security_stamp` mới, active, hết khóa; verify `VerifyHashedPassword=Success`).
- **File thay đổi chính:** `Domain/Enums/Enums.cs`, `Domain/Entities/{TrainingRecord,TrainingEvaluation,LoanRepayment}.cs (mới)` + `{Loan,AgentCommission,Candidate}.cs`, `Infrastructure/Persistence/{ApplicationDbContext,DbSeeder,PermissionRegistry,ApplicationDbContextFactory (mới)}.cs` + `Constants/RoleNames.cs` + 3 migration, `Web/Display/{Labels,PersonTitle (mới)}.cs`, `Web/Commissions/CommissionEngine.cs (mới)`, `Web/Identity/AgentScope.cs`, `Web/Storage/{IDocumentStorage,MinioDocumentStorage}.cs` (audio), `Web/wwwroot/js/audio-recorder.js (mới)`, `Web/Components/Pages/{Training/* (mới), Loans/{Loans,LoanDialog,DebtCollection (mới)}, Candidates/{CandidateDetail,Candidates,StudentAccountDialog (mới)}, Finance/Finance, Agents/{Agents,AgentsTree (mới)}, Portal/MyCommissions, JobOrders/JobOrders, Leads/Leads, Messages/Messages}.razor`, `Components/Layout/NavMenu.razor`.
- **Đã test:** **`dotnet build Polymind.slnx` in-place = 0 warning / 0 error.** Web CHẠY `http://localhost:5177` `/login` **200**, `/js/audio-recorder.js` **200** (4 export). DB (psql) xác nhận **3 migration** trong `__EFMigrationsHistory` + 3 bảng mới (`training_records/training_evaluations/loan_repayments`) + cột (`loans.kind/monthly_deduction_amount`, `agent_commissions.stage`, `candidates.owner_user_id`) + role `parent/student` + 5 quyền `training:*`. Reset 13 acc CTV verify `VerifyHashedPassword=Success` (hash giữ nguyên sau seeding). Smoke routes mới `/debt-collection` `/agents/tree` = 302 (redirect login, không 500). **Chưa duyệt mắt bằng trình duyệt thật** (nhất là ghi âm — cần cấp quyền micro).
- **Lưu ý/cảnh báo cho người sau:** **CHƯA commit** (chờ user duyệt). **1C đổi hành vi:** hoa hồng nay phát sinh khi ĐÓNG TIỀN (không phải advance bước) — dữ liệu cũ giữ nguyên (`Stage=null`); tổng tiền không đổi, chỉ đổi thời điểm; **% từng giai đoạn Vietgroup chưa gửi** → dùng % config mốc cũ áp lên full cost. **Học viên/Phụ huynh dùng chung `Candidate.OwnerUserId`** (1 owner/ứng viên) — UI hiện tạo tài khoản **Học viên**; Phụ huynh tạo qua `/admin` role `parent` (=quyền CTV) rồi set OwnerUserId nếu cần xem con; owner chưa gắn → KHÔNG thấy ứng viên nào (an toàn). **Ghi âm** truyền bytes qua `IJSStreamReference` (né giới hạn 32KB SignalR) — nếu đổi sang trả base64 phải nới `MaximumReceiveMessageSize`. Acc CTV chưa bật 2FA → bị ép cài Authenticator lần login tới. **Design-time factory `ApplicationDbContextFactory`** để `dotnet ef` chỉ build Infra (né khóa Web đang chạy).
- **[[project-polymind-deploy-plan]]**

### [2026-07-03] Session 55 — Claude — Mục 5: Quy trình 20 bước + bảng thi đua theo tháng đặt cọc + sửa 2FA lệch + docx phản hồi
- **Làm được:** (1) **Quy trình 17→20 bước** theo góp ý Vietgroup: `WorkflowStep` mở rộng thành 20 bước + bước phụ **7.5 ReselectJobOrder**; GIỮ NGUYÊN tên member cũ (Consulting→"Đã tư vấn", Orientation→"Học tiếng/định hướng/nghề", Completed→"Hoàn thành quy trình") nên dữ liệu cũ (lưu text) **tự map, KHÔNG cần migration** — đã verify 11 giá trị `current_step` trong DB đều là member hợp lệ. Thêm member mới: Contacted(B2), CoeApplication(B12), OverseasSupport(B19). (2) Helper mới `Display/WorkflowSteps.cs` (No→"7.5", Next bỏ qua 7.5, Progress theo 20 bước chính) — thay mọi chỗ ép `(int)step` / `/17`. (3) `CandidateDetail.razor`: stepper 20 bước (chèn 7.5 khi đang ở đó), **tình huống theo bước**: B8 chọn 1/3 (Thi tuyển|Phỏng vấn|Xét duyệt) + nút **Rớt→lùi 7.5**; B7.5 bắt buộc gắn **đơn hàng MỚI khác đơn cũ** mới sang B8 (giữ hồ sơ); B10 chọn nhiều nội dung học hoặc Bỏ qua; B15 chọn Thanh toán hoàn tất|Cam kết trả nợ (ràng buộc có hồ sơ vay); B19 **nhật ký tương tác** (record InProgress, ghi nhiều lần, dài hạn); B20 chỉ chuyển khi **vay đã tất toán** (thêm `LoanStatus.Settled`) + xác nhận thủ công. Chống trùng hoa hồng khi đổi đơn ở 7.5 (bỏ lọc JobOrderId trong `EnsureCommissionsAsync`). (4) **Bảng thi đua `/agents`**: cột "Ứng viên" nay đếm **ứng viên đạt bước Đặt cọc trong tháng đang lọc** (từ `WorkflowStepRecords` step=Deposit, CompletedAt trong tháng) — sang tháng mới reset 0, cùng thời điểm phát sinh hoa hồng. (5) Rà & sửa "17 bước"→"20 bước" + WorkflowSteps.No/Progress ở Candidates/Home/Reports/AiAssistant/Guide/LeadDetail/StepChip; Loans + LoanDialog thêm trạng thái "Đã tất toán". (6) **Sửa giao diện 2FA lệch trái** (`login-2fa` + `2fa-setup`): thẻ đặt trực tiếp trong `.login-page` bị rule desktop `.login-card{margin:0}` mất canh giữa → thêm wrapper `.login-solo` (flex justify-center, canh giữa mọi breakpoint PC/laptop/iPad/mobile), cache-bust `login.css?v=20260703-2fa-center`. (7) **File docx phản hồi Vietgroup**: `POLYMIND - Phan hoi gop y Vietgroup Edu.docx` ở root repo — bám đúng 4 mục góp ý gốc, mỗi ý nêu "Góp ý → Đã cập nhật những gì", không thêm/bớt ý nào.
- **File thay đổi chính:** `src/Polymind.Domain/Enums/Enums.cs` (WorkflowStep 20 bước + LoanStatus.Settled), `src/Polymind.Web/Display/{WorkflowSteps.cs (mới),Labels.cs,WorkflowStepAccess.cs}`, `Components/Shared/StepChip.razor`, `Components/Pages/Candidates/{CandidateDetail,Candidates}.razor`, `Components/Pages/Agents/Agents.razor`, `Components/Pages/{Home,Reports/Reports,Ai/AiAssistant,Guide/Guide,Leads/LeadDetail}.razor`, `Components/Pages/Loans/{Loans,LoanDialog}.razor`, `Components/Account/{LoginTwoFactor,TwoFactorSetup}.razor`, `Components/App.razor`, `wwwroot/css/login.css`, `src/Polymind.Infrastructure/Persistence/DemoDataSeeder.cs` (bỏ 7.5 khỏi seed random), `POLYMIND - Phan hoi gop y Vietgroup Edu.docx (mới)`.
- **Đã test:** `dotnet build Polymind.slnx` = **0 warning / 0 error**. Web `http://localhost:5177` `/login` = **200**, `/css/login.css` = **200** (có `.login-solo`), HTML có cache-bust `login.css?v=20260703`. DB Postgres: `candidate_job_orders.current_step` 11 giá trị đều map đúng nhãn 20 bước mới. Docx đã trích text kiểm đủ 4 mục + bảng 20 bước.
- **Lưu ý/cảnh báo cho người sau:** **CHƯA commit.** Chưa duyệt mắt trình duyệt thật luồng 20 bước (B8 rớt→7.5→gắn đơn mới, B19 nhật ký, B20 gate vay) + 2FA canh giữa trên điện thoại — cần circuit MudBlazor, Ctrl+F5 lấy CSS mới. Bước phụ 7.5 KHÔNG nằm trong `WorkflowSteps.Main` (dùng cho dropdown lọc/stepper) — chỉ hiện khi hồ sơ đang ở đó. Nếu sau này cần migration đổi TÊN member thì phải UPDATE chuỗi text trong DB (hiện chưa cần). `LoanStatus.Settled` là điều kiện chốt B20 — hồ sơ có vay chưa Settled sẽ bị chặn ở B19.

<!-- Session 52-54b (Codex/Claude — icon menu partner, nhận diện tài khoản đối tác, góp ý Vietgroup đợt 1, 2FA TOTP bắt buộc) đã cắt bớt để giữ 6 entry gần nhất. -->


