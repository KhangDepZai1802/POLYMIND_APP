# 📓 WORKLOG — POLYMIND OLMS (file phối hợp giữa các session AI)

> **File này là nguồn sự thật chung cho Claude và Codex.** Đọc TRƯỚC khi làm, cập nhật SAU khi làm.

---

## ⚠️ QUY TẮC BẮT BUỘC (đọc mỗi lần)

1. **TRƯỚC khi bắt đầu session:** đọc hết file này — phần `TRẠNG THÁI HIỆN TẠI` + `VIỆC TIẾP THEO` + entry mới nhất trong `NHẬT KÝ`.
2. **Chọn việc:** làm theo `VIỆC TIẾP THEO`. Nếu muốn làm khác, ghi rõ lý do vào nhật ký.
3. **SAU khi làm xong (hoặc trước khi hết session):**
   - Cập nhật `TRẠNG THÁI HIỆN TẠI` (1-2 dòng).
   - Cập nhật `VIỆC TIẾP THEO` (việc kế cho người sau — càng cụ thể càng tốt).
   - **Thêm 1 entry mới** vào đầu `NHẬT KÝ SESSION` (mới nhất ở trên).
   - **GIỮ GỌN (bắt buộc):** chỉ giữ **6 mục gần nhất** ở `TRẠNG THÁI HIỆN TẠI` và **6 entry gần nhất** ở `NHẬT KÝ SESSION`. Khi thêm mục/entry mới → **xóa mục/entry cũ nhất** để file không phình to (đỡ tốn token đọc lại mỗi phiên).
   - Nếu có blocker/lỗi chưa fix → ghi vào `BLOCKERS`.
4. **Luôn để app build được** (`dotnet build Polymind.slnx`) trước khi kết thúc. Nếu để dở, ghi rõ "đang dở, chưa build".
5. Chi tiết backlog đầy đủ + bẫy kỹ thuật + quy ước code: xem **docs/05-handoff-codex.md** (đọc 1 lần để nắm nền).

**Format 1 entry nhật ký:**
```
### [YYYY-MM-DD] Session N — <Claude|Codex>
- **Làm được:** ...
- **File thay đổi chính:** ...
- **Đã test:** ... (build? chạy? smoke test gì?)
- **Lưu ý/cảnh báo cho người sau:** ...
```

---

## 🔒 RÀNG BUỘC NGHIỆP VỤ — CHỐT VỚI USER 2026-07-10 (BẮT BUỘC — Claude + Codex phải tuân)

> 7 yêu cầu user đã chốt (kèm quyết định qua hỏi–đáp). **Trạng thái: ĐÃ CODE XONG (Session 63) — BUILD 0/0, CHƯA smoke test trình duyệt (Docker/DB local chưa chạy) → cần duyệt mắt.** KHÔNG tự đổi quyết định đã chốt; nếu vướng, hỏi user trước.

**✅ RB-1 — Ẩn thông tin nhạy cảm CTV với Phụ huynh/Học sinh.** Với role `parent` và `student`, mọi nơi hiển thị thông tin CTV (card/modal "Thông tin cộng tác viên") PHẢI ẩn đúng 2 dòng: **"Ứng viên đã giới thiệu"** và **"Tỷ lệ hoa hồng CTV %"**. Các dòng khác (tên, đại lý, SĐT, email, trạng thái) vẫn hiện. Kiểm tra qua `AgentScope.IsParent/IsStudent`.

**✅ RB-2 — Chỉ super_admin đổi TVV/CTV + card Job trên chi tiết ứng viên.**
- Chỉ `super_admin` được đổi **TVV (consultant)** và **CTV (collaborator)** đã gắn vào ứng viên; role khác chỉ xem.
- Thêm **card riêng "Đơn hàng (Job)"** trên trang chi tiết ứng viên (giống card chọn job lúc convert lead→ứng viên). **1 ứng viên = 1 job active** (đổi job = thay job cũ).
- Đổi job / đổi TVV / đổi CTV: chỉ super_admin, mỗi thao tác PHẢI qua hộp xác nhận **"Chắc chưa?" + nhập lại mật khẩu đăng nhập của CHÍNH super admin** (reuse `ConfirmPasswordDialog`; verify bằng `SignInManager.CheckPasswordSignInAsync` với user hiện tại). Ghi audit cho mỗi lần đổi.

**✅ RB-3 — Tìm kiếm theo tên ở trang quản lý tài khoản Phụ huynh & Học sinh.** Trang admin `/admin/parents-students` (`ParentStudentAccounts.razor` / `AccountManagerPanel`): thêm ô **search theo tên** (lọc họ tên, có thể kèm email). (Không phải search trong portal của PH/HS.)

**✅ RB-4 — Đổi mật khẩu Phụ huynh/Học sinh.** User `parent`/`student` **tự đổi mật khẩu** trong portal (`/me`), dùng Identity — **KHÔNG lưu plaintext**. Super admin **chỉ có nút "Đặt lại mật khẩu"** (đã có ở `AccountManagerPanel` — bảo đảm chạy được), **KHÔNG xem được** mật khẩu user tự đặt. (User đã chọn phương án an toàn thay vì lưu plaintext.)

**✅ RB-5 — Giữ dữ liệu AI trong 1 phiên đăng nhập.** Hội thoại AI + kết quả trích xuất CV/ảnh PHẢI **lưu server-side theo `UserId`**, sống sót khi chuyển chức năng **và khi F5/refresh**; chỉ **mất khi đăng xuất**. Không để state chỉ trong component (mất khi điều hướng). Gợi ý: bảng/cache theo user + nạp lại khi mở trang AI + dọn khi logout (hook logout endpoint).

**✅ RB-6 — Bấm thông báo → tới đúng trang nguồn.** Click 1 thông báo (chuông + trang thông báo) PHẢI điều hướng theo `Notification.ReferenceType` + `ReferenceId`: lead→`/leads/{id}`, candidate→`/candidates/{id}`, agent→`/agents/{id}`, payment/finance→trang tài chính ứng viên, visa→`/visas/...`, v.v.; đánh dấu đã đọc khi click. Thiếu reference thì không điều hướng.

**✅ RB-7 — Bổ sung thông báo cho các module (user chốt: Tài chính, Hoa hồng & Đại lý, Visa & Xuất cảnh; KHÔNG thêm nhóm Tài khoản/Bảo mật).** (Visa/Xuất cảnh đã có sẵn; bổ sung mới: hoa hồng chờ duyệt, kỳ trả nợ vay đến hạn, khoản chi chờ duyệt.)
- **Tài chính** → Kế toán + Director/super_admin: khoản thu đến hạn/quá hạn, công nợ ứng viên, khoản chi chờ duyệt, khoản vay (Loan) đến hạn trả.
- **Hoa hồng & Đại lý** → CTV/Đại lý liên quan + Kế toán: hoa hồng phát sinh → chờ duyệt → đã chi.
- **Visa & Xuất cảnh** → Visa staff + RM: nộp/bổ sung/kết quả visa, lịch phỏng vấn sắp tới, lịch bay sắp tới, xác nhận xuất cảnh.
- Mọi thông báo mới PHẢI set `ReferenceType`/`ReferenceId` để RB-6 điều hướng được.

---

## 🎯 TRẠNG THÁI HIỆN TẠI

- **2 CHỈNH SỬA THEO YÊU CẦU USER (Session 64, Claude) — CODE XONG, BUILD 0/0, CHƯA restart/duyệt mắt/commit.** (1) **Đổi đơn hàng = RESET quy trình 20 bước.** Trên chi tiết ứng viên, khu "Đổi đơn hàng (chỉ Super Admin)": khi super admin đổi sang đơn hàng mới, `ChangeJobOrderAsync` nay set `CurrentStep = WorkflowStep.Lead` (B1) + `Status = Active` + **xóa toàn bộ `WorkflowStepRecords`** của CJO đó → tiến trình bắt đầu sạch từ đầu (trước đây RB-2 giữ nguyên tiến trình). Cập nhật caption + text xác nhận mật khẩu + snackbar + audit (log cả `CurrentStep` cũ/mới). **Lưu ý:** chỉ reset workflow, KHÔNG hoàn tiền/hoa hồng đã phát sinh. (2) **Bỏ Đại lý & CTV khỏi dropdown vai trò ở trang Quản trị.** `AccountManagerPanel` thêm param `CreatableRoles` (mặc định = `ManagedRoles`) tách "vai trò được tạo/gán qua dropdown" khỏi "vai trò hiển thị/quản lý"; dropdown tạo + dropdown chuyển vai trò dùng `CreatableSet`; tài khoản mang role ngoài `CreatableSet` vẫn hiện nhưng coi như **cố định** (chip khóa, không đổi role). `Admin.razor` truyền `CreatableRoles=StaffCreatableRoles` (= StaffRoles trừ agent/collaborator) → trang `/admin` KHÔNG còn tạo/đổi sang Đại lý/CTV (chỉ tạo ở trang Đại lý & Hoa hồng `/agents/{id}`); trang P&S không đổi (CreatableRoles null → fallback ManagedRoles).
- **BỎ AUTHENTICATOR/TOTP 2FA CHO TOÀN BỘ TÀI KHOẢN TEST (Session 62, Codex) — CODE XONG, BUILD 0/0, CHƯA restart/redeploy/commit.** Đã gỡ luồng Google/Microsoft Authenticator khỏi web hiện tại: `Login.razor` chỉ dùng email+mật khẩu + lockout, không rẽ `/login-2fa`; xóa middleware enforcement, `TwoFactorEnforcement`, trang `/login-2fa`, `/account/2fa-setup`, nút/logic “Đặt lại 2FA” trong dialog sửa user; bỏ config `Security:RequireTwoFactor`; cache-bust login CSS. **Báo cáo `POLYMIND_Báo cáo tiến độ.docx` đã cập nhật:** giai đoạn đối tác test tạm bỏ 2FA để 2–3 người dùng chung tài khoản demo; bản final trước vận hành thật sẽ triển khai 2FA bằng SMS gửi về số điện thoại người dùng nhập (do SMS tốn phí nên chưa bật lúc test).
- **CỔNG CÁ NHÂN HÓA PHỤ HUYNH & HỌC SINH (mobile-first) + 4 việc phát sinh (Session 60, Claude) — CODE XONG, BUILD 0/0, MIGRATION ĐÃ ÁP, WEB ĐANG CHẠY :5177, CHƯA duyệt mắt/commit.** (1) **DB:** thêm `Candidate.ParentUserId` + migration `AddCandidateParentUser` (đã áp — cột `parent_user_id` tồn tại) → 1 ứng viên gắn tối đa **3 tài khoản** (CTV/TVV, Học sinh `OwnerUserId`, Phụ huynh `ParentUserId`). (2) **Scope/quyền:** `AgentScope` resolve owned candidate qua `OwnerUserId||ParentUserId` + cờ `IsParent/IsStudent/OwnedCandidateHasLoan`; **DbSeeder** đổi quyền parent+student = `candidates/training/payments/loans/notifications:read + messaging` (parent **bỏ** agents/commissions — verify DB OK). (3) **Điều hướng mobile:** `NavMenu` nhánh self-scoped (Tổng quan/Hồ sơ/Đào tạo/Tài chính/Hỗ trợ vay[nếu vay]/Tin nhắn/AI, bỏ Hướng dẫn+admin), **bottom-nav** `PortalBottomNav.razor` (CSS ≤959px) trong MainLayout, `RedirectToLogin` đưa self-scoped về `/me`. (4) **Trang `/me` Tổng quan MỚI** (`Pages/Portal/Overview.razor`): thẻ XKLĐ/đóng tiền/đào tạo/job/vay. (5) **Cá nhân hóa:** `Candidates`+`Training` self-scoped nhảy thẳng chi tiết (bỏ bảng); `Finance`+`Loans` chỉ hồ sơ của mình; guard lọc cứng `OwnedCandidateId`. (6) **Tin nhắn theo quan hệ:** parent↔CTV/TVV+con; student↔CTV/TVV+phụ huynh (`BuildAllowedRecipientsAsync` + chặn server ở Send). (7) **AI giới hạn:** bỏ tab CV, chỉ nạp hồ sơ CHÍNH ứng viên + prompt cấm lộ dữ liệu người khác/ngoài phạm vi. (8) **CandidateDetail:** khu "Tài khoản đăng nhập" 3 dòng + `ParentAccountDialog` (audit link_parent/unlink_parent). **PHÁT SINH:** (a) **Sửa ghi âm** (`audio-recorder.js` trả **Blob** → hết lỗi "not a typed array or blob"; thêm khung **nghe lại** trước Gửi/Bỏ, KHÔNG tự gửi). (b) Trang Ứng viên **cột "Trạng thái"** (Ứng viên/Học viên/Lao động theo `PersonTitle`) + Giới tính **ẩn chữ giữ icon ở laptop** (`laptop-hide-inline` ≤1600px). (c) **Đào tạo mở rộng:** Training list + card ở CandidateDetail hiện cho **mọi ứng viên đã đặt cọc** (`PersonTitle.IsTitled`) → 14 người thay vì 3. (d) **Gemini/Render:** thông báo lỗi key nêu đặt env `Ai__Gemini__ApiKey` trên Render (key production PHẢI set ở dashboard Render, KHÔNG commit). (e) **Tạo tài khoản Đại lý/CTV** ở `/agents/{id}`: nút "Tạo tài khoản đại lý" ở đầu trang + nút "Tạo tài khoản" từng hàng CTV (`PartnerAccountDialog.razor` — role agent/collaborator, set `Agent/Collaborator.UserId`, audit link_account); đã có tài khoản → nút **disable "Đã có tài khoản"** (verify DB: 5/5 đại lý + 13/13 CTV đã có tài khoản → tất cả disable). **TINH CHỈNH cuối phiên:** (f) `RedirectToLogin` đưa Đại lý/CTV về `/agents`, Phụ huynh/Học sinh về `/me` → **hết cảnh non-super-admin thấy "Không có quyền truy cập" sau đăng nhập** (partner/self-scoped landing "/" trước đây bị access-denied). (g) CSS bảng ứng viên: thu gọn padding cột (chữ đỡ xuống dòng) + laptop ≤1600px **chip giới tính ép về hình tròn** (hết méo icon). (h) Trang `/admin/parents-students` **GIỮ nút vào + bảng quản lý**, chỉ **ẩn card "Tạo tài khoản"** (thêm param `AccountManagerPanel.ShowCreateForm=false`) vì tạo trực tiếp ở chi tiết ứng viên. (i) Overview: tài khoản Phụ huynh chào **"Phụ huynh em <tên>"**. Tin nhắn parent/student **không** thấy super admin (đã đúng từ đầu). **ĐÃ ĐỒNG BỘ DB LÊN RENDER** (`sync-db-to-render.ps1` — Render 41 users, có cột `parent_user_id` + quyền mới). ⚠️ **CODE mới CHƯA lên Render** (chỉ DB) → phải redeploy app từ git để thấy cổng cá nhân hóa + fix landing; và đặt env `Ai__Gemini__ApiKey` trên Render.
- **TRANG RIÊNG "PHỤ HUYNH & HỌC SINH" (Session 59, Claude) — CODE XONG, BUILD 0/0, WEB ĐANG CHẠY :5177, CHƯA duyệt mắt/commit.** Tách phần quản lý tài khoản thành component dùng chung `AccountManagerPanel.razor` (tham số `ManagedRoles` + `RolesFixed` + `AllowDelete`). Tab **Tài khoản** ở `/admin` giờ quản lý mọi role TRỪ parent/student (`StaffRoles`) + có nút **"Phụ huynh & Học sinh"** → trang mới **`/admin/parents-students`** (`ParentStudentAccounts.razor`) chỉ tạo/quản lý parent+student. **Cập nhật theo yêu cầu user:** trang P&S **KHÔNG khóa cố định** nữa — cho **đổi qua lại parent↔student** (đổi role có hộp `ConfirmPasswordDialog` xác nhận); super admin có nút **Xóa** tài khoản (`AllowDelete=true`, luôn hỏi xác nhận `ShowMessageBoxAsync`, dọn `Candidate.OwnerUserId` trước khi `UserManager.DeleteAsync`, audit `delete`). Chốt chặn 2 chiều vẫn còn ở `CreateUserAsync`/`SaveUserRoleAsync` (không tạo/đổi ngoài `ManagedRoles`). **Không migration.**
- **ĐỒNG BỘ LOGIC TIỀN BẠC 3 TRANG: THU NỢ = CHỈ NỢ CÔNG TY (Session 58, Claude) — CODE XONG, BUILD 0/0, CHƯA restart/commit.** User làm rõ luồng tiền: (1) **Tài chính** giữ nguyên = nghĩa vụ đóng theo chi phí đơn (4 bước 20/30/30/20), **thủ công**, không tự động đánh dấu dù công ty ứng tiền. (2) **Vay ngân hàng** = người vay tự trả cho NH → **KHÔNG** lên trang Thu nợ; `LoanDialog` khi chọn *Ngân hàng* nay **ẩn** trừ lương/lịch trả góp, chỉ lưu thông tin (số tiền, NH, lãi, ngày giải ngân); lưu bank sẽ dọn `MonthlyDeduction/DeductionStart` + xóa `LoanRepayment` cũ. (3) **Nợ công ty** = tỉnh không cho NH vay, công ty cho vay để đóng tài chính, sau XKLĐ trả lại **gốc + lãi** → chỉ loại này hiện ở `/debt-collection` (lọc `Kind==Company`); số tiền nợ **prefill = chi phí đơn hàng** (cho sửa); lịch trả góp nay chia **gốc + lãi đơn** (lãi = gốc × %/năm × số tháng/12). **Không migration** (chỉ đổi logic UI + query). PHẢI restart web để nạp code.
- **8 GÓP Ý DUYỆT MẮT (Session 57, Claude) — CODE XONG, COMPILE 0/0, CHƯA restart/commit.** (1) Lỗi micro: `audio-recorder.js` bắt lỗi getUserMedia → message tiếng Việt rõ. (2) Chip **Học viên** (đơn du học) / **Lao động** (nhóm khác) theo `JobOrder.Category`, giữ ngưỡng B5 (`PersonTitle.cs`). (3) **MODULE ĐÀO TẠO** riêng: `/training` (danh sách + tiến độ) + `/training/{id}` (**timeline theo tuần** gom `TrainingEvaluation`), NavMenu "Đào tạo"; thẻ Đào tạo ở CandidateDetail rút gọn còn tóm tắt + link module. (4) **Nợ VG**: seeder tạo Loan `Kind=Company` (tỉnh không được NH vay, VG cho đi) + lịch trả góp → `/loans`+`/debt-collection`. (5) Lỗi thông báo trùng khóa 23505: lead-care nay **UPDATE hàng cũ** thay vì chèn (`NotificationService`). (6) Hoa hồng: đại lý **5%** (1/1.5/2.5% theo mốc), CTV **35%** (30-40%) — `AgentCommissionRates.cs` + seeder chuẩn hóa dữ liệu cũ + tính lại. (7) Bỏ menu "Thông báo" (giữ chuông góc phải). (8) Tràn bảng laptop: `WorkflowSteps.ShortLabel`+`StepChip` 2 span + CSS ≤1600px hiện nhãn rút gọn. **Không migration** (code+seeder). **PHẢI restart web** để nạp code + seeder chuẩn hóa hoa hồng/nợ VG.
## ⏭️ VIỆC TIẾP THEO (baton — làm cái này trước)

**#-1 — DUYỆT MẮT 2 CHỈNH SỬA Session 64 (CODE XONG, BUILD 0/0, CHƯA commit):**
- Chi tiết ứng viên (super_admin) → khu "Đổi đơn hàng": đổi sang đơn mới → xác nhận mật khẩu → tiến trình phải nhảy về **B1 (Lead)**, timeline sạch, `WorkflowStepRecords` cũ đã xóa. Kiểm cả ứng viên đang ở bước giữa (VD B8/B15).
- Trang `/admin` tab Tài khoản: form "Tạo tài khoản" dropdown vai trò **KHÔNG còn Đại lý/CTV**; dropdown "Chuyển vai trò" cũng không có; tài khoản Đại lý/CTV đang có (nếu hiện) chip **"Cố định"**. Trang `/admin/parents-students` không đổi. Tạo Đại lý/CTV vẫn ở `/agents/{id}`.


**#0 — RESTART/REDEPLOY + TEST LẠI ĐĂNG NHẬP KHÔNG 2FA (Session 62 — CODE XONG, BUILD 0/0, CHƯA restart/redeploy/commit):**
- Restart web local hoặc redeploy Render để nạp code mới. Nếu web cũ đang chạy `:5177`, cần tắt/chạy lại rồi Ctrl+F5 lấy `login.css?v=20260709-login`.
- Test các tài khoản demo dùng chung mật khẩu `Admin@123` như `admin@polymind.local`, `ctv-ctv0001@polymind.local`, tài khoản Học sinh/Phụ huynh demo: sau email+mật khẩu phải vào thẳng app/landing theo role, **không** xuất hiện `/login-2fa` hoặc `/account/2fa-setup`.
- Vào `/admin` → sửa tài khoản: dialog chỉ còn họ tên/email/mật khẩu mới, không còn khối “Xác thực 2 lớp/Đặt lại 2FA”.
- Mở `POLYMIND_Báo cáo tiến độ.docx` kiểm mục “Bảo mật nâng cao” đã ghi đúng: hiện tạm bỏ 2FA để đối tác test; bản final sau này dùng SMS OTP về số điện thoại người dùng nhập.
- Khi user duyệt xong → commit gộp các thay đổi chưa commit gần đây; khi đưa vào sử dụng thật, thiết kế SMS OTP mới (provider SMS, chi phí, lưu số điện thoại, rate limit, audit) thay cho TOTP Authenticator cũ.

**#1 — TIẾP TỤC DUYỆT MẮT CỔNG CÁ NHÂN HÓA PHỤ HUYNH/HỌC SINH (Session 60 — CODE XONG, BUILD 0/0, MIGRATION ĐÃ ÁP):**
- Tạo/gắn tài khoản test ở chi tiết ứng viên đã đặt cọc (gợi ý **UV-20260608-2001 Lê Hữu Khang**), rồi đăng nhập Học sinh/Phụ huynh để kiểm `/me`, bottom-nav mobile, dữ liệu self-scoped, tin nhắn theo quan hệ và AI không lộ hồ sơ người khác. Lưu ý mới: **không cần Authenticator** khi test.
- Không hồi quy admin/nhân sự: `/candidates` có cột Trạng thái; laptop ≤1600px cột Giới tính chỉ còn icon; `/training` hiện ứng viên đã đặt cọc; ghi âm tin nhắn có nghe lại trước Gửi/Bỏ.
- Render: DB đã sync nhưng code mới còn phải redeploy; nhớ đặt env `Ai__Gemini__ApiKey` trên Render trước/hoặc cùng lúc deploy.

## 🚧 BLOCKERS / NỢ KỸ THUẬT

- **Smoke UI local Session 53:** Docker Desktop/Linux engine chưa chạy; `docker compose up -d` lỗi `dockerDesktopLinuxEngine ... The system cannot find the file specified`, nên chưa mở được `http://localhost:5177/login` để click kiểm tài khoản Đại lý/CTV. Code đã build xanh; bật Docker Desktop rồi smoke lại.
- Nợ triển khai thật: cần `.env.production` với secret thật, chứng chỉ TLS thật (ưu tiên Caddy tự cấp Let's Encrypt cho DuckDNS; Nginx manual cert vẫn là fallback), SMTP/provider SMS/Zalo thật nếu bật gửi ngoài InApp.
- **CẢNH BÁO DEPLOY PUBLIC:** Không public production nếu còn user mẫu/mật khẩu `Admin@123`. DuckDNS/No-IP chỉ giải quyết DNS động; vẫn cần public IP hoặc port-forward 80/443 hoạt động. Nếu mạng công ty bị CGNAT/không mở port được thì không cố DuckDNS, chuyển fallback tunnel.
- ~~Module placeholder chờ làm thật~~ — ĐÃ XONG HẾT (finance/agents/visa/reports đều có trang thật).

---

## 📜 NHẬT KÝ SESSION (mới nhất ở trên)

### [2026-07-10] Session 64 — Claude — Reset quy trình 20 bước khi đổi đơn hàng + bỏ Đại lý/CTV khỏi dropdown vai trò trang Quản trị
- **Yêu cầu user:** (1) trên chi tiết ứng viên, sau khi đổi sang đơn hàng mới thì quy trình 20 bước phải reset lại từ đầu. (2) tài khoản Đại lý & CTV hiện tạo được ở cả trang Đại lý & Hoa hồng lẫn trang Quản trị → chỉ muốn tạo ở trang Đại lý & Hoa hồng, nên xóa Đại lý/CTV khỏi dropdown vai trò trong trang Quản trị.
- **Làm được:** (1) `ChangeJobOrderAsync` (CandidateDetail): đổi `JobOrderId` xong nay set `CurrentStep=WorkflowStep.Lead` + `Status=Active` + xóa hết `WorkflowStepRecords` của CJO → tiến trình reset về B1 sạch (đảo lại hành vi giữ nguyên tiến trình của RB-2); cập nhật caption UI, message ConfirmPasswordDialog, snackbar, audit (log `CurrentStep` cũ/mới). (2) `AccountManagerPanel` thêm param `CreatableRoles` (mặc định = ManagedRoles) tách vai trò "tạo/gán qua dropdown" khỏi "hiển thị/quản lý"; `_assignableRoles` + guard `CreateUserAsync`/`SaveUserRoleAsync` dùng `CreatableSet`; `IsRoleFixed` thêm nhánh `IsRoleUnassignable` → role ngoài CreatableSet hiện chip "Cố định" (hint "quản lý ở trang riêng"). `Admin.razor` thêm `StaffCreatableRoles` (=StaffRoles trừ agent/collaborator) + truyền `CreatableRoles`.
- **File thay đổi chính:** `Components/Pages/Candidates/CandidateDetail.razor`, `Components/Pages/Admin/AccountManagerPanel.razor`, `Components/Pages/Admin/Admin.razor`, `WORKLOG.md`.
- **Đã test:** `dotnet build src/Polymind.Web` = **0 warning / 0 error**. Không migration (chỉ UI + logic + query). CHƯA restart/duyệt mắt trình duyệt (Docker/DB local chưa chạy) → cần duyệt mắt + smoke.
- **Lưu ý/cảnh báo cho người sau:** reset đơn hàng CHỈ đụng workflow (CurrentStep/Status/WorkflowStepRecords), KHÔNG hoàn tiền/hoa hồng đã phát sinh — nếu user muốn hoàn cả tài chính thì phải mở rộng. Trang P&S (`ParentStudentAccounts`) không truyền CreatableRoles → fallback ManagedRoles, hành vi cũ giữ nguyên. Tài khoản Đại lý/CTV cũ vẫn hiển thị ở `/admin` để xem/khóa nhưng role bị khóa cố định (không tạo/đổi ở đây).

### [2026-07-10] Session 63 — Claude — Thực thi 7 RÀNG BUỘC NGHIỆP VỤ mới (RB-1→RB-7)
- **Làm được:** (RB-1) `CollaboratorInfoDialog` inject `AgentScope`, ẩn 2 dòng "Ứng viên đã giới thiệu" + "% hoa hồng CTV" khi role parent/student. (RB-2) `CandidateDetail`: thêm card riêng "Đơn hàng của ứng viên" (mã/quốc gia/công ty/tiến trình) + khu super_admin đổi TVV/CTV và đổi đơn hàng, mỗi thao tác qua `ConfirmPasswordDialog` (nhập lại mật khẩu super admin) + audit `reassign_people`/`change_job_order`; 1 ứng viên = 1 job active. (RB-3) `AccountManagerPanel` thêm ô tìm theo tên/email (lọc GroupedUsers) → áp cho `/admin/parents-students`. (RB-4) `ChangePasswordDialog.razor` mới (tự đổi MK qua `UserManager.ChangePasswordAsync`, KHÔNG lưu plaintext) + mục "Đổi mật khẩu" trong user-menu `MainLayout`; super admin vẫn reset qua `UserEditDialog` sẵn có. (RB-5) `AiSessionStore` singleton lưu hội thoại + kết quả trích xuất CV theo `UserId` (sống qua chuyển trang + F5), xóa ở endpoint `/Account/Logout`; `AiAssistant` đọc/ghi qua store. (RB-6) `NotificationService.ResolveTargetUrlAsync` map ReferenceType→route (lead/candidate/payment/visa/flight/commission/loan/loan_repayment/expense); `Notifications.razor` click → mark read + điều hướng. (RB-7) thêm 3 `NotificationType` (ReminderLoanRepayment, ExpenseApproval, CommissionPending) + nhắc: hoa hồng chờ duyệt, kỳ trả nợ vay đến hạn, khoản chi chờ duyệt (kế toán+giám đốc) + nhãn/icon/màu ở `Labels`.
- **File thay đổi chính:** `Components/Shared/CollaboratorInfoDialog.razor`, `Components/Shared/ChangePasswordDialog.razor` (mới), `Components/Pages/Candidates/CandidateDetail.razor`, `Components/Pages/Admin/AccountManagerPanel.razor`, `Components/Layout/MainLayout.razor`, `Components/Pages/Notifications/Notifications.razor`, `Components/Pages/Ai/AiAssistant.razor`, `Ai/AiSessionStore.cs` (mới), `Notifications/NotificationService.cs`, `Display/Labels.cs`, `Domain/Enums/Enums.cs`, `Program.cs`, `WORKLOG.md`.
- **Đã test:** `dotnet build Polymind.slnx` = **0 warning / 0 error**. CHƯA smoke test trình duyệt vì Docker Desktop/Postgres local chưa chạy (`docker ps` trống) → cần bật Docker rồi duyệt mắt.
- **Lưu ý/cảnh báo cho người sau:** 3 enum NotificationType mới lưu dạng string → KHÔNG cần migration. RB-4: đổi MK làm mới security-stamp; do RevalidationInterval=30' user có thể bị buộc đăng nhập lại trong vòng 30' — chấp nhận được. RB-2 đổi job chỉ hoán `JobOrderId` trên CJO active, giữ nguyên tiến trình (override hành chính, khác luồng B7.5). RB-5 store in-memory (mất khi restart app — đúng nghĩa "trong phiên"). Cần duyệt mắt: modal CTV với tài khoản parent/student, nút đổi TVV/CTV/job (nhập MK), tìm tên ở /admin/parents-students, đổi MK ở user-menu, giữ hội thoại AI sau F5, click thông báo điều hướng đúng.

### [2026-07-09] Session 62 — Codex — Bỏ Authenticator/TOTP 2FA cho toàn bộ tài khoản test + cập nhật báo cáo tiến độ
- **Làm được:** Gỡ luồng Authenticator/TOTP khỏi bản web hiện tại để đối tác có thể test chung tài khoản: login chỉ còn email+mật khẩu, không rẽ `/login-2fa`; bỏ middleware ép cài 2FA; xóa trang `/login-2fa`, `/account/2fa-setup`, `TwoFactorEnforcement`; bỏ nút/logic “Đặt lại 2FA” trong dialog sửa user. Cập nhật `POLYMIND_Báo cáo tiến độ.docx`: hiện tạm bỏ 2FA trong giai đoạn test, bản final sẽ dùng SMS OTP gửi về số điện thoại người dùng nhập.
- **File thay đổi chính:** `src/Polymind.Web/Components/Account/Login.razor`, `src/Polymind.Web/Program.cs`, `src/Polymind.Web/Components/Pages/Admin/UserEditDialog.razor`, `src/Polymind.Web/Components/App.razor`, `src/Polymind.Web/wwwroot/css/login.css`, xóa `LoginTwoFactor.razor`/`TwoFactorSetup.razor`/`TwoFactorEnforcement.cs`, `src/Polymind.Web/appsettings.json`, `POLYMIND_Báo cáo tiến độ.docx`, `WORKLOG.md`.
- **Đã test:** `dotnet build Polymind.slnx` = **0 warning / 0 error**. Chưa restart web/chưa smoke login bằng trình duyệt sau build.
- **Lưu ý/cảnh báo cho người sau:** DB Identity vẫn có cột `two_factor_enabled` theo schema mặc định/migration cũ, nhưng code hiện tại không dùng TOTP Authenticator. Khi làm bản final SMS OTP, triển khai luồng mới riêng (provider SMS, số điện thoại, chi phí, chống spam/rate-limit, audit), không khôi phục TOTP cũ trừ khi user yêu cầu.

### [2026-07-09] Session 61 — Claude — Báo cáo tiến độ (.docx) + FIX DỨT ĐIỂM landing access-denied cho non-super-admin
- **FIX landing (quan trọng):** trên Render, tài khoản KHÔNG phải super admin (Phụ huynh/Học sinh, Đại lý/CTV) đăng nhập vào là kẹt ở `/access-denied?ReturnUrl=%2F`. Nguyên nhân: sau login điều hướng `/` (Home có `[Authorize(dashboard:read)]`) → các role này KHÔNG có `dashboard:read` → **cookie-middleware** (không phải router Blazor) đá sang `/access-denied`, và nút "Về tổng quan" lại trỏ `/` → lặp vô hạn. `RedirectToLogin` (Session 60) chỉ xử lý nhánh router `<NotAuthorized>`, KHÔNG bắt được nhánh middleware này. **Đã sửa `AccessDenied.razor`:** khi authenticated → đọc `AgentScope`, `IsSelfScoped` → `/me`, `IsPartnerOnly` → `/agents` (forceLoad), chỉ nhân sự nội bộ (đều có dashboard:read) mới thấy trang thông báo + nút về `/`; anonymous → nút Đăng nhập. Đã verify DbSeeder: CHỈ parent/student (self) + agent/collaborator (partner) thiếu `dashboard:read` → fix bao trọn đúng nhóm bị kẹt, không che lỗi quyền thật của nhân sự. Build `src/Polymind.Web -t:Compile` = **0/0**. Cần **push để Render auto-deploy** mới hết trên production.

- **Yêu cầu user:** dựa theo WORKLOG + `POLYMIND HỌP 3_7_2026.pdf`, làm 1 file docx **`POLYMIND_Báo cáo tiến độ.docx`** tóm tắt trang trọng những gì đã làm cho đối tác Vietgroup — gồm cả các phần làm THÊM ngoài biên bản họp; thêm mỗi role 1 tài khoản demo để họ test từng nghiệp vụ.
- **Làm được:** Sinh `POLYMIND_Báo cáo tiến độ.docx` ở root (python-docx, script ở scratchpad `gen_report.py`). Bố cục: bìa 2 logo (POLYMIND.png + VIETGROUP.png) + thư ngỏ + tổng quan nhanh; ánh xạ 5 nhóm biên bản họp (Tài chính/Công nợ, Ứng viên/Đào tạo, Đại lý/CTV, App Demo=ngoài phạm vi, Lỗi/Urgent) theo dạng "Yêu cầu → ✔ đã làm"; mục ★ "Các hạng mục chủ động thực hiện thêm" (cổng cá nhân hóa PH/HS, 2FA, quy trình 20 bước, AI, 3 tài khoản/ứng viên...); "Đề nghị phối hợp" (Vietgroup gửi data quy trình học + quy ước hoa hồng); PHỤ LỤC bảng 10 tài khoản demo theo role (Admin@123) + note tài khoản PH/HS tạo ở chi tiết ứng viên + cảnh báo 2FA lần đầu.
- **File thay đổi chính:** `POLYMIND_Báo cáo tiến độ.docx` (mới, root). KHÔNG đụng code app.
- **Đã test:** mở lại bằng python-docx OK (96 paragraph, 5 bảng, bảng tài khoản 11 hàng). Không build (không đổi code).
- **Lưu ý/cảnh báo cho người sau:** đã TẠO TAY 2 tài khoản demo gắn cùng hồ sơ **UV-20260608-2001 (Lê Hữu Khang)**: Học sinh `hocvien-uv202606082001@polymind.local` (`owner_user_id`) + Phụ huynh `phuhuynh-uv202606082001@polymind.local` (`parent_user_id`), role student/parent, mật khẩu `Admin@123` (copy `password_hash` của admin — salt nhúng nên verify OK), `two_factor_enabled=false` → lần đầu login bị ép cài 2FA. Chèn bằng psql `docker exec -i polymind-postgres` (nhớ cờ **-i** mới nhận stdin heredoc). CTV demo là `ctv-ctv0001@polymind.local`. Nếu đổi nội dung báo cáo → sửa `gen_report.py` trong scratchpad rồi chạy lại.

### [2026-07-09] Session 60 — Claude — Cổng cá nhân hóa Phụ huynh/Học sinh (mobile-first) + tài khoản Đại lý/CTV + sửa ghi âm + cột Trạng thái ứng viên
- **Yêu cầu user (nhiều đợt trong 1 phiên):** 2 role Phụ huynh/Học sinh phải thành cổng khách hàng cá nhân hóa (ưu tiên điện thoại): xem thẳng hồ sơ của mình thay vì bảng, có Tổng quan/Tài chính/Hỗ trợ vay riêng, Tin nhắn giới hạn đúng người liên quan, AI chỉ hỏi-đáp về mình; 1 ứng viên gắn 3 tài khoản (CTV/TVV + Học sinh + **Phụ huynh mới**). Phát sinh: sửa lỗi ghi âm tin nhắn (không tự gửi, nghe lại trước khi gửi); thêm cột Trạng thái (Ứng viên/Học viên/Lao động) + laptop ẩn chữ giới tính; Đào tạo mở cho mọi ứng viên đã đặt cọc; nút tạo tài khoản Đại lý/CTV ở `/agents/{id}` (đã có thì disable); Gemini lỗi key trên Render.
- **Làm được:** (chi tiết đầy đủ ở TRẠNG THÁI HIỆN TẠI mục Session 60). Tóm tắt: migration `AddCandidateParentUser`; `AgentScope`+`DbSeeder` (parent bỏ agents/commissions); `NavMenu`+`PortalBottomNav`+`RedirectToLogin`; trang `/me` (`Portal/Overview.razor`); cá nhân hóa Candidates/Training/Finance/Loans (nhánh self-scoped, guard `OwnedCandidateId`); tin nhắn theo quan hệ (`Messages.BuildAllowedRecipientsAsync`); AI giới hạn (`AiAssistant` restricted prompt + own-data); `ParentAccountDialog`+`PartnerAccountDialog`; ghi âm trả **Blob** + khung nghe lại; cột Trạng thái + `laptop-hide-inline` cho giới tính; `PersonTitle.IsTitled` mở rộng Training.
- **File thay đổi chính:** `Domain/Entities/Candidate.cs` + migration `*_AddCandidateParentUser`, `Infrastructure/Persistence/{ApplicationDbContext,DbSeeder}.cs`, `Web/Identity/AgentScope.cs`, `Web/Components/Layout/{NavMenu,MainLayout,PortalBottomNav(mới)}.razor`, `Web/Components/RedirectToLogin.razor`, `Web/Components/Pages/{Portal/Overview(mới),Candidates/{Candidates,CandidateDetail,ParentAccountDialog(mới)},Training/{Training,TrainingDetail},Finance/Finance,Loans/Loans,Messages/Messages,Ai/AiAssistant,Agents/{AgentDetail,PartnerAccountDialog(mới)}}.razor`, `Web/wwwroot/js/audio-recorder.js`, `Web/wwwroot/app.css`, `Web/Ai/GeminiClient.cs`.
- **Đã test:** `dotnet build src/Polymind.Web` = **0 warning/0 error** (build nhiều lần trong lúc code). Migration đã áp (DB có cột `parent_user_id`). Web CHẠY `http://localhost:5177` `/login` **200**, không lỗi khởi động. DB verify: quyền parent/student = candidates/training/payments/loans/notifications:read + messaging (parent hết agents/commissions); 14 ứng viên đã đặt cọc (Training sẽ hiện ~14); **5/5 đại lý + 13/13 CTV đã có tài khoản** → nút tạo tài khoản disable đúng. **Chưa duyệt mắt trình duyệt thật** (đặc biệt bottom-nav mobile + ghi âm cần micro + đăng nhập parent/student bị ép 2FA). **Chưa commit.**
- **Lưu ý/cảnh báo cho người sau:** parent/student đăng nhập lần đầu bị **ép cài 2FA** (enforcement toàn hệ thống) — báo user cần Authenticator khi test. AI self-scoped nạp hồ sơ CHÍNH ứng viên + prompt cấm lộ dữ liệu khác — nếu đổi model/prompt giữ nguyên ràng buộc này. **Key Gemini production KHÔNG commit** — phải đặt env `Ai__Gemini__ApiKey` ở dashboard Render. Ghi âm nay trả **Blob** qua `IJSStreamReference` (ổn định hơn Uint8Array) + data URL để nghe lại (clip ngắn, base64 trong DOM). IDE auto-format có thể hoàn tác edit → Read lại trước mỗi Edit.
- **[[project-polymind-olms]]** · **[[project-polymind-gemini-ai]]**

### [2026-07-07] Session 59 — Claude — Trang riêng "Phụ huynh & Học sinh" (tách khỏi trang Tài khoản, vai trò cố định)
- **Yêu cầu user:** ở Quản trị hệ thống thêm nút **"Phụ huynh & Học sinh"** → trang riêng **giống y chang** trang Tài khoản nhưng CHỈ quản lý tài khoản phụ huynh/học viên. Trang Tài khoản **không tạo được** role parent/student; trang Phụ huynh & Học sinh **chỉ tạo được** parent/student (không role khác). Role phụ huynh/học sinh **cố định**, không đổi sang role khác.
- **Làm được:** (1) Tách toàn bộ phần quản lý tài khoản (form tạo + bảng theo vai trò + tạo/sửa/khóa/đổi role) thành component dùng chung **`AccountManagerPanel.razor`** (tham số `ManagedRoles` + `RolesFixed`) → 2 trang chắc chắn giống hệt nhau, không lệch. Panel chỉ tạo/hiển thị/đổi trong phạm vi `ManagedRoles` (chốt chặn cả ở `CreateUserAsync`/`SaveUserRoleAsync`); khi `RolesFixed=true` mọi hàng hiện chip **"Cố định"** (như super admin) — ẩn dropdown role + nút "Lưu role", chỉ còn Sửa/Khóa. (2) `Admin.razor` tab Tài khoản nay dùng `<AccountManagerPanel ManagedRoles="@StaffRoles" />` (StaffRoles = mọi role trừ parent/student) + nút **"Phụ huynh & Học sinh"** ở PageHeader → `/admin/parents-students`; đã xóa code account cũ (LoadUsers/CreateUser/SaveRole/Toggle/RoleOrder/GroupedUsers/UserRow) khỏi Admin.razor (giữ tab Phân quyền + Nhật ký). (3) Trang mới **`ParentStudentAccounts.razor`** `/admin/parents-students` (gate `users:read`) = `<AccountManagerPanel ManagedRoles="{parent,student}" RolesFixed="true" />` + nút quay về Quản trị.
- **File thay đổi chính:** `Web/Components/Pages/Admin/AccountManagerPanel.razor (mới)`, `Web/Components/Pages/Admin/ParentStudentAccounts.razor (mới)`, `Web/Components/Pages/Admin/Admin.razor`.
- **Đã test:** `dotnet build src/Polymind.Web` sau khi dừng host cũ = **0 warning / 0 error**. Restart web, `/login` = **200**. **Không migration** (chỉ UI). **Chưa duyệt mắt trình duyệt thật** + **chưa commit**.
- **Lưu ý/cảnh báo cho người sau:** IDE (VS/OmniSharp) đang mở file có auto-format **hoàn tác một phần edit giữa chừng** → nếu sửa Admin.razor tiếp phải Read lại trước mỗi Edit. Trang này chỉ quản lý **tài khoản** (tạo user + gán role) — việc **gắn học viên với hồ sơ ứng viên** vẫn qua `StudentAccountDialog`/`Candidate.OwnerUserId` ở CandidateDetail (không đổi). `RolesFixed` chặn đổi role kể cả giữa parent↔student (đúng "cố định"); muốn cho đổi qua lại thì bỏ `RolesFixed` cho panel P&S.

